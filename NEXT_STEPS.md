# 提升翻譯正確率的路線圖（交接文件）

最後更新：2026-08-12
適用對象：接手本專案語言模型端的人或 agent。
定位：**每一項建議都附實測依據**；已證實無效的做法也列出，避免重做。

---

## 0. 先讀懂這件事：EM 的理論上限只有約 23%

在留存的 167 句語料庫長句上，量化「參考 Gloss 有多少詞能從中文字面推得」：

| | 比例 |
|---|---:|
| 可從中文推得 | 77.7% |
| **無法推得**（需上下文／聾人慣用知識） | **22.3%** |
| **參考答案「每個詞都可推得」的句子** | **22.8%** |

→ **77% 的句子在缺少前文時理論上不可能逐句翻對**。
例：「聾人和重聽者能看到手語翻譯和字幕」的參考 Gloss 含「電視」，
該詞不在中文句中，來自**前一句**的話題。

**因此：不要拿 EM 對照 100% 來評斷模型**，那會嚴重誤導。
現況 v11 在此測試集 EM 1.2%、ROUGE-L 50.08；可努力的空間是 1.2% → ~23%，
不是 → 70%。

---

## 1. 目前狀態（v11）

| 測試集 | 句數 | 是否見過 | EM | ROUGE-L | 可播放率 |
|---|---:|---|---:|---:|---:|
| `test`（核心，問候語為主） | 33 | 否 | 69.70% | 82.71 | 91.67% |
| `test_corpus`（語料庫長句） | 167 | **否** | **1.20%** | 50.08 | 66.96% |
| `test_papers`（論文例句） | 143 | **否** | **13.29%** | 57.08 | 78.20% |
| 〔對照〕語料庫 60 句 | 60 | **是** | 21.67% | 69.43 | 65.16% |

**關鍵對照**：同為語料庫長句，訓練集內 21.67% vs 未見 1.20%
→ 先前 v8→v10 的「進步」主要是記憶效果。

**跨測試集穩定、真實習得的能力**：疑問類型 93–99%、否定 96–97%、
有效 JSON 99–100%、可播放率貼齊參考天花板（Gap −0.13）。

> **2026-08-13 參考答案修正**：`test_papers` 有 39 句的參考答案把論文的
> **替代詞**（`看/ *見/ *欣賞`）串成了 Gloss 序列，等於把不合語法的寫法當成
> 標準答案。修正解析後（[`extract_paper_examples.py`](scripts/extract_paper_examples.py)
> 的 `clean_gloss_line`）重算：EM 11.19%→**13.29%**、ROUGE-L 55.23→**57.08**、
> BLEU-4 16.51→**17.71**。
> 論文例句**全部落在 `test_papers`**（train/dev 各 0 筆），故本修正不動訓練
> 資料、不需重訓、也不需重跑推論——輸出未變，只是換了參考答案離線重算
> （`scripts/recompute_from_split.py`）。
> ⚠️ 此規則**只適用於論文來源**。語料庫的 `?` 是它自己的疑問句尾標記
> （`你/傘/帶來/有沒有?`，train 41 句、dev 5 句、test_corpus 2 句），套用同一
> 規則會刪掉這些標記，而疑問類型正確率正是少數穩定習得的能力。

### 1.1 三層診斷（2026-08-12，v12）：病因是**詞彙覆蓋**，不是語序或記憶

`results/three_tier_report.md`；重跑：`scripts/build_eval_tiers.py` →
`scripts/eval_three_tier.py --ple gpu`。

| 層級 | n | EM% | TokenF1 | GER | BLEU-4 |
|---|---:|---:|---:|---:|---:|
| A. Seen（訓練原句） | 100 | 42.0 | 0.814 | 0.230 | 68.86 |
| B. Paraphrase（換句話說） | 100 | 28.0 | 0.780 | 0.316 | 56.69 |
| C. Unseen（未見句） | 100 | 13.0 | 0.662 | 0.573 | 20.24 |

**三個推翻先前假設的結論：**

1. **模型沒有在背訓練資料** —— Seen 層 EM 僅 42%，看過兩個 epoch 的原句仍有
   58% 無法逐字複現。上表第 38 行「進步主要是記憶效果」的說法**過度簡化**：
   落差真實存在，但成因不是記憶。
2. **模型不是靠中文字面對應** —— 改寫層 TokenF1 只掉 4.2%。9 句「中文字面
   完全失去依據」的硬題 GER 反而更好（0.286 vs 0.319，n=9 不足下統計結論）。
3. **語序不是瓶頸** —— 語序錯誤在三層都只有 0–5%。未見層最大錯誤是表外詞
   （33%），且其中 71% 是直接照抄中文原句（Seen 層 45%）。

⚠️ **度量校正**：上述 OOV 是以**訓練詞彙**為基準，會把「合法但訓練沒出現的
手語詞」（畫家、幼稚園、目不轉睛）誤判為造詞。改以詞彙總表為基準後，
未見層 45 個表外詞中 18 個（40%）其實合法，**真造詞是 27 個不是 45 個**。
判定表外詞請用「詞彙總表 ∪ 訓練詞彙」（見 `scripts/gloss_fallback.py`）。

---

## 2. 建議做的（依證據強度排序）

### ~~1. 上下文翻譯（SCOPE 路線）~~ — **已做，效果邊際，不建議再投入**

**2026-08-12 實測結論（v12 vs v11，同一份 167 句留存測試集）**：

| 指標 | v11（無前文） | v12（有前文） | 變化 |
|---|---:|---:|---|
| Exact Match | 1.20% | 1.80% | +0.6pp（2 句→3 句，統計上是雜訊） |
| ROUGE-L | 50.08 | 50.83 | +0.75 |
| BLEU-4 | 14.83 | 15.87 | +1.04 |
| 疑問類型正確 | 93.41% | 89.82% | **−3.6pp（退步）** |

遠不及 SCOPE 論文的 +3 BLEU，且訓練成本翻倍（2h22m vs 1h07m）。
**本節原標為「★ 唯一能突破 23% 上限的方法」，該評級已下修——證據不支持。**
若仍要試，未驗證的假設是「前文應給**前一句的 Gloss** 而非中文」
（SCOPE 用的是前文翻譯結果）。

⚠️ 上下文模型推論時**必須**帶入前文，否則訓練/推論不一致會嚴重低估表現
（`eval_json_model.py`、`serve_model.py` 皆已處理）。

### 〔原第 1 項的原始依據，保留供參〕

**依據**：22.3% 的參考詞需要上下文；SCOPE（AAAI-25，教授指定參考文獻）
實測上下文帶來 +3 BLEU；本專案亦證實受詞前置與話題性相關。

**資料現成**：`data/tslcorpus/parallel.jsonl` 的 `seg_uuid` 即段落分組，
`corpus_id` 尾碼（如 `G2D1P1/a81`）遞增即句序，**檔案原始順序就是句序**
（已驗證）。407 段、平均每段 13 句。

**做法**：把前 1–2 句中文（或其 Gloss）加入輸入。
注意：`test_corpus` 是依對話群組整組留存，故 test 句的前文也在 test 內，
不會造成洩漏。

### ★ 新第 1 優先：擴充詞彙覆蓋 — 三層診斷唯一支持的資料擴充方向

**依據**：未見層 33% 的錯誤是表外詞，其中 71% 是照抄中文（見 1.1 節）。

**但要補的是詞彙，不是更多同源長句。** 2026-08-12 逐一驗證各來源：

| 來源 | 結論 |
|---|---|
| 文化部語料庫 | 已全爬完（407 段/5,272 句），無可再爬 |
| **中正辭典（3,500 詞）** | **死路**。例句原始資料只有 546 句、已抽出 544；3,500 詞條**全是同形對應**（中文＝Gloss），訓練它只會強化照抄中文；別名 950 組中 **448 組（47%）與語料庫衝突**（辭典說「聾人→聾」但語料庫用「聾人」308 次）；剩 502 組無衝突者**修不到 28 個問題詞中的任何一個**，且 **27/28 個問題詞根本不在此辭典內** |
| **教育部常用手語辭典** | **唯一有效的新來源**：涵蓋 28 個問題詞中的 8 個（診所、社區、鬧鐘、寶寶、警報、概念、安定、畫家）。爬蟲見 `scripts/scrape_moe_signdict.py`。⚠️ **授權未查證**（站上為 All Right Reserved，無開放資料聲明），納入訓練或散布前須先確認 |
| 手語教學影片 | 只有中文字幕、無 Gloss 標註，**構不成平行語料** |

### 2. 中文端改寫增強（ITRI 路線）

**依據下修**：原依據是「訓練集內 21.67% vs 未見 1.20% ＝ 模型在背字面」。
三層診斷已推翻該推論（改寫層 TokenF1 只掉 4.2%，模型並非靠字面對應）。
本項仍可做——擴充句型多樣性對未見層有理論價值——但**不應以「攻擊記憶化」
為理由**，那個問題不存在。

**做法**：同一 Gloss 目標配多種中文說法（5,272 × 3 ≈ 15k）。
**注意**：改寫需保證語意不變，否則會製造錯誤標籤。

### 3. 人工評估（計畫 6.2）— 工具已備妥，待執行

盲測 A/B 評分表產生器：`scripts/make_human_eval_sheet.py`。
模型輸出與語料庫參考以 A/B 呈現、順序由句子雜湊決定，評分者不知孰為機器
產出（避免權威偏誤）；對照 key 另存不交付。

模型輸出常是**可理解的直譯**，只是不夠母語化：

```
氣象預報說昨天晚上發布了海上颱風警報
  ref : 氣象/宣布/昨天++/晚上/海/臺灣      ← 高度壓縮、聾人慣用
  pred: 天氣/預報/說/昨天/晚上/發布/海上颱風/警報  ← 接近直譯
```

**這算錯嗎？只有母語者能判定。** EM 無法回答，需 5 分制人工評分。

---

## 3. 已證實無效，不要重做

| 做法 | 實測結果 |
|---|---|
| 再爬更多語料庫長句 | 語料庫已全爬完（407 段/5,272 句）；且失敗句多已在訓練集內 |
| 爬中正辭典補詞彙 | 例句只有 546 句已全抽；詞條全同形；別名 47% 與語料庫衝突；修不到任何一個實測問題詞（見第 2 節表） |
| 上下文翻譯（SCOPE） | v12 實測僅 +0.6pp EM／+1.04 BLEU，疑問類型反退 3.6pp，訓練成本翻倍 |
| 表外詞 fallback 提升準確率 | 可播放率僅 +0.93pp（表外詞只佔輸出詞約 7%，天花板就這麼高）。它的價值在讓失敗明確可處理，不是提升準確率 |
| 加大 LoRA 容量 r=16→32 | dev loss 反而略差（0.7268 vs 0.7061），v7 實驗 |
| RAG 檢索增強 | 對**未見**句子有害（EM 75.76%→69.70%）；只對訓練集內的句子有效 |
| 提高 epochs | 各版皆 epoch 1–2 最佳，之後過擬合 |
| 修 device_map offload 以外的推論加速 | 見下方效能備註 |

---

## 4. 重要工程備註（踩過的坑）

- **推論效能**：`device_map` 只要含任何 `"cpu"` 項目，accelerate 就為整個模型
  掛 offload hook，**每 token 慢到 35 秒**。務必用 `--ple gpu`
  （`device_map={"":0}`，0.06 秒/token，快 580 倍）。
- **`--ple auto` 的陷阱**：可用顯存 10.13GB 剛好卡在 10.1GB 門檻時會誤退回
  慢速模式。評估與服務一律明指 `--ple gpu`。
- **背景啟動要用絕對路徑**：`cd X && ... & disown; grep 相對路徑` 會在錯誤的
  cwd 找檔案，導致誤判「沒啟動」。
- **共用生產 GPU**：另有 root 服務與 `~/0804try` 專案；**別的 session 起的服務
  不可自行 kill，要先問使用者**。
- **標籤品質會以意外方式擴散**：WH 判定把「幾」當疑問詞，不只讓 NMS 錯，
  還因規則「疑問詞置句末」把「前幾天」搬到句末，同時汙染語序。

---

## 5. 資料與腳本地圖

| 用途 | 指令 |
|---|---|
| 產生切分（含兩個留存測試集） | `python3 scripts/split_data.py --use-all --length-balance --papers-as-test --corpus-test-ratio 0.12 --corpus-test-min-len 6` |
| 產生 JSON 目標 | `python3 scripts/build_json_targets.py --splits train dev test test_corpus test_papers` |
| 訓練 | `python3 scripts/train_qlora.py --output outputs/XXX --target json --epochs 2 --batch 2 --grad-accum 4 --max-len 192 --lr 2e-4 --seed 42` |
| 評估 | `python3 scripts/eval_json_model.py --adapter XXX --tag YYY --test-file test_corpus.jsonl --ple gpu` |
| 起服務 | `python3 scripts/serve_model.py --adapter XXX --target json --port 8018 --ple gpu` |
| 語料庫輸出稽核 | `python3 scripts/audit_corpus_outputs.py --n 60 --tag ZZZ` |
| **三層診斷** | `python3 scripts/build_eval_tiers.py` 然後 `python3 scripts/eval_three_tier.py --adapter XXX --ple gpu` |
| **fallback 離線量測** | `python3 scripts/eval_fallback_offline.py` |
| **人工評估表（盲測）** | `python3 scripts/make_human_eval_sheet.py --model-tag v12` |
| **教育部辭典爬取** | `python3 scripts/scrape_moe_signdict.py`（授權未查證，資料不入版控） |

**宣稱界線**：對外報告一律用 `test_corpus` / `test_papers` 的數字；
核心 33 句僅作 Stage A/B 歷史對照。任何「語料庫指標」須標註是否在訓練集內。

---

## 6. 方法上的坑（比工程坑更容易犯，且不會報錯）

- **不要用測試集的錯誤來設計修復規則。** fallback 初版的「中文虛詞刪除表」
  是直接看未見層錯誤手工列出的，等於拿測試集調參，同批重測必然虛高。
  該規則已整條移除。
- **token 層 P/R/F1 對語序完全無感。** 把答案整個顛倒，F1 仍是 1.000
  （實測 GER 0.901）。任何「有沒有退化」的判準都必須同時看 GER。
- **判定「模型造詞」的基準會決定結論。** 用訓練詞彙當基準會高估 67%
  （45 個 vs 實際 27 個）。用「詞彙總表 ∪ 訓練詞彙」。
- **辭典與語料庫衝突時，以語料庫為準。** 中正辭典 47% 的別名與語料庫實際
  用法衝突；先前「唸書/讀書」之爭也是語料庫證據勝出。
- **A/B 對照要用同一批句子。** 三層診斷的 Seen 與 Paraphrase 刻意用同一批
  100 句，差距才能歸因於「換句話說」而非句子難度。

# Unsloth 導入與「手語腳本」訓練格式評估

2026-08-20 ／ 對應教授兩項指示：①改用 Unsloth 裝設語言模型 ②訓練資料改成候選挑選格式

---

## 摘要

兩件都做了實測，不是紙上評估。

**Unsloth 可行，建議採用。** 在學校 VM（RTX 4060 Ti 16GB）實測 Gemma 4 E4B QLoRA
峰值顯存 10.87 GB、模型載入 17 秒（自製管線約 2 分鐘）、訓練 3.5 秒/步。
產出的是標準 PEFT adapter，現有服務端能直接載入，不必改部署。

**腳本格式可行，但有兩個必要的前置建設**，都已做出可運作版本：一張 16,628 筆的
sign_id 總表，和一支訓練／上線共用的候選檢索器。過程中量到一個教授應該知道的
成本數字：原始格式 k=25 就要 688 token，已超過目前訓練用的 max_len 512。

---

## 一、Unsloth 導入

### 實測結果

| 項目 | 數值 | 對照 |
|---|---|---|
| 峰值顯存（E4B QLoRA、max_len 512） | **10.87 GB** | 卡上限 16 GB |
| 模型載入 | 17.4 秒 | 自製管線約 2 分鐘 |
| 訓練速度 | 3.5 秒/步（batch 2×grad_accum 8） | — |
| loss 下降 | 5.27 → 0.70（30 步） | 正常 |
| adapter 格式 | 標準 PEFT LoRA | 服務端 peft 0.19.1 可解析 |

### Unsloth 幫我們解掉的既有工程負債

7 月為了讓 E4B 在這張共用卡上訓起來，`scripts/train_qlora.py` 手工處理了
PLE 逐層嵌入的顯存問題、自訂遮罩 collator、regex 挑 LoRA target 等約 200 行。
這些在 Unsloth 都是框架內建，換過去後訓練碼縮到約 40 行標準寫法，
且未來換模型不必重踩。

### 安裝踩到的三個坑（都已解決，寫下來供重建環境用）

1. **系統 Pillow 太舊** — `PIL.Image.Resampling` 不存在，往 venv 裝 `pillow>=10`。
2. **torchao 版本衝突** — Unsloth 帶的 torchao 0.18 需要 torch≥2.8，
   系統是 2.7.1，降到 `torchao==0.12.0`。會有版本警告，QLoRA 走 bitsandbytes
   用不到 torchao 新功能，無實質影響。
3. **⚠️ Gemma 4 的 chat template 標記與 Gemma 3 不同** — 是 `<|turn>model\n`
   而非 `<start_of_turn>model\n`。用錯**不會報錯**，只會把整批 label 遮成 -100
   然後靜默退回全序列 loss，訓練照跑但目標錯了。這個坑最危險，
   建議任何人接手時先驗證 `train_on_responses_only` 的日誌有印出成功訊息。

### 環境與資源協調

裝在獨立 venv `~/unsloth-venv`（不動現有 `.venv`，那是踩了十幾個坑才調通的
transformers 5.5.4 組合，混裝必炸）。

顯存分配的實務結論：訓練需要 10.87 GB，故 **v11 服務（9.4 GB）必須停**；
台語 STT（1.8 GB）與 TTS（3.6 GB）合計 5.4 GB 理論上可共存
（16 − 10.87 = 5.5 GB），但邊際只有 100 MB，保守做法是訓練時一併停。
台語 TTS 是 Docker 容器 `fatchord-wavernn_hapsing_1`（有 autoheal 看顧，
必須 `docker stop` 而非 kill）；STT 是 systemd 服務 `stt-service`。

### 匯出

三條路都可用，建議依序：

1. **標準 PEFT adapter**（約 140 MB）— 現有 `serve_model.py` 直接載，風險最小。
2. **合併權重**（約 15 GB）— 服務端少一步，但每版佔磁碟。
3. **GGUF**（`q4_k_m`）— 給 Ollama／llama.cpp，部署到沒有訓練環境的機器。
   E4B 是多模態架構，GGUF 匯出相容性需實測；量化法與訓練時不同，
   匯出後要重跑評估確認分數沒掉。

---

## 二、教授指定的訓練資料格式

### 這個格式在做什麼

把任務從「自由生成 gloss」改成「從候選清單挑 sign_id」。對本專案是對症下藥：
v4 診斷過模型輸出約三成是動作庫演不出來的詞，改成候選約束後，
輸出不可播放詞在結構上就被堵死；缺詞時輸出 `needs_review=true` 而非幻覺。

### 已完成的三支程式

| 檔案 | 作用 |
|---|---|
| `scripts/build_sign_inventory.py` | 建 sign_id 總表 |
| `scripts/sign_candidates.py` | 候選檢索器（訓練／上線共用） |
| `scripts/build_script_dataset.py` | 轉成 `tsl-script-v1` 格式 |

### sign_id 總表

**16,628 個手語**，錨定在虛擬人動作庫 `lexicon.json` 實際有影片的詞
——不是辭典詞條數。中正辭典有 3,500 詞不代表虛擬人演得出 3,500 詞，
`asset_class: natural_playable` 必須有事實依據。

| 來源 | 數量 |
|---|---|
| 教育部常用手語辭典 | 7,913 |
| 文化部臺灣手語語料庫 | 3,412 |
| 台灣手語地名網 | 846 |
| 台灣手語姓氏網 | 565 |
| tasli | 425 |
| 其他 | 3,467 |

每筆帶影片檔與起訖秒數（無一筆缺時間軸）。ID 採「既有永不變動、新詞接續編號」
的原則——ID 一旦寫進訓練資料與模型權重就不可改。

**待教授確認**：目前用 `TSL_01084` 這種流水號。教授範例用語義代號
（`TSL_TODAY`），可讀性好但 16,628 個詞無法全取英文名。
建議流水號為主、高頻核心詞另給語義別名。

### 候選檢索器：五個通道

候選**只從中文生成，絕不看正解**。拿正解回頭湊候選會讓訓練與上線分布不一致，
模型學到的約束上線即失效。

1. 字面命中（所有匹配子字串，非最長匹配切分）
2. 例句遷移（相似訓練句用過的手語）
3. 詞對齊表（從平行語料挖中文片段↔gloss 共現）
4. 高頻核心手語（每題恆常提供）
5. 字元重疊干擾項

### 過程中發現並修掉的四個問題

**① 候選檢索的自我洩漏。** 第一版 train 涵蓋率 96.8%、dev 只有 73.4%。
原因是相似句檢索把句子自己撈回來，等於把答案直接塞進候選。會訓練出
「照抄檢索結果」的捷徑，上線沒有捷徑就失效。加留一法後 train 掉到 80.0%
——這才是誠實數字。

**② 最長匹配吃掉短詞。** 「這個東西」只切出 這個／東西，但語料正解是「這」；
「有沒有」切成一塊，「有」進不了候選。改成收錄所有匹配子字串。

**③ 空間指示與代名詞靠檢索撈不到。** 這是最有意思的發現：

```
往西邊走會到公園  →  西/那/走/那/公園/那
他推我下去池子裡  →  池/這/池/他/我/他/我/他/推
```

中文句面完全沒有「那」字。這類是自然手語的**空間指示與代名詞**，
屬語法機制而非中文詞的對譯，任何以中文為輸入的檢索都撈不到，
但它們正是最常缺的詞（這 285 次、什麼 220 次、那 181 次）。
解法是把高頻核心手語恆常放進候選——手語者隨時都能比指示與代名詞，
候選清單也就該隨時備著。

**④ 標註記號未正規化。** `買++` 是「買」打兩次的重複記號，不是新手語；
未正規化就查表會把演得出來的詞誤判成缺片（dev 集虛報 43 個缺口）。
改用既有的 `norm()` 處理。

### 涵蓋率實測

四項修正累積下來的改善（k=40）：

| 切分 | 句數 | 詞涵蓋率（初版→最終） | 整句可拼出 | needs_review |
|---|---|---|---|---|
| train | 8,992 | 80.0% → **92.4%** | 73.9% | 26.1% |
| dev | 666 | 72.7% → **79.1%** | 50.1% | 49.9% |
| test（核心） | 33 | 96.2% → **97.5%** | 93.9% | 6.1% |
| test_corpus | 167 | 63.1% → **69.4%** | 15.6% | 84.4% |
| test_papers | 143 | 71.0% → **74.4%** | 41.3% | 58.7% |

候選數 k 的取捨（dev 集實測）：k=25 → 77.6%、k=40 → 79.9%、k=60 → 82.9%。
k=40 是性價比轉折點（見下節 token 成本）。

**train 高於 dev 是預期的**：詞對齊表與核心詞表都由 train 建成，
對 train 而言仍屬「見過的資料」。**dev 的 79.1% 才是誠實的泛化估計**。

**重要對照**：舊格式的實測可播放率是 69.98%（v4 診斷，584 句）。
新格式因為候選只從有影片的手語裡挑，涵蓋率反而更高——這個格式
在可播放性上是進步而非退步。

殘餘缺口分兩類，意義完全不同：
- **庫裡真的沒有** → 排補片，對接 `data/video/video_gap.json`。
  正規化修好後這類大幅下降（dev 84→18、test_corpus 47→10），
  代表先前多數「缺片」是誤報，真正要錄的比想像中少。
- **庫裡有但檢索沒撈到** → 語義距離遠的翻譯選擇（我學會了→領悟、
  味道都沒變→一樣、訴說→講述/侃侃而談），部分不可約化。這是目前最大缺口。

test_corpus 偏低（69.4%）合理：那是未經整理的真實對話語料，
句子長、口語成分多、空間指示密集，本來就是最難的一批。

### ⚠️ Token 成本：教授需要知道的實務限制

用 Gemma 4 tokenizer 實測（dev 60 句，k=40）：

| 寫法 | token 中位數 | 說明 |
|---|---|---|
| A 教授原格式 | 1,015 | `{"sign_id":"TSL_01084","gloss":"今天"}` |
| B 壓縮候選 | **654** | `"TSL_01084=今天"`，省 36% |
| C 壓縮＋短 ID | 520 | 再去掉 `TSL_` 前綴，省 49% |

原格式 k=25 就要 688 token，**已超過目前訓練用的 max_len 512**。
序列長度直接吃顯存與訓練時間（注意力是平方級）。

**建議採 B**：語意與教授的 schema 完全相同，只是候選序列化方式不同，
保留 `TSL_` 前綴讓 ID 自我識別（下游除錯需要），
同樣 token 預算下可從 k=25 提升到 k=40，換得更高涵蓋率。

### 對 schema 的一個設計改進

`needs_review=true` 的負例**不需要人工造假**。原本的想法是刻意抽掉候選來製造，
但實際上檢索撈不到正解裡的手語時，那句就真的拼不出來——自然產生負例，
並在 `oov_items` 列出缺詞，還能直接對接補片工作表排錄影順序。
這比人工抽樣更真實，也更能反映上線時的失敗樣態。

---

## 三、後續建議順序

1. 教授確認 sign_id 命名規則與是否採壓縮候選格式（B）
2. 用新格式訓練，max_len 調到 768，評估指標改為：
   sign_id 序列的 BLEU/ROUGE、**約束違反率**（輸出候選外的 ID，理想 0%）、
   **needs_review 的 precision/recall**
3. 補片優先序依 `oov_not_in_library` 統計排（庫裡真缺的才需錄影）
4. 檢索端持續改進：語義距離遠的對應是目前最大缺口

## 誠實邊界

- 涵蓋率是「候選清單能不能拼出參考答案」，不等於翻譯品質；
  參考答案本身也只是一種合法譯法。
- 本文所有數字為程式實測，但模型在新格式下的實際表現尚未訓練驗證。
- gloss 層可驗證不等於母語者確認自然道地；NMS／手形／地區變體仍屬影片軌。

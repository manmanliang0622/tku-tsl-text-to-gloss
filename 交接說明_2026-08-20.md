# 交接說明（貼給 Claude 用）

> 用法：把「以下貼給 Claude」整段複製貼上，Claude 就有足夠脈絡接手。
> 本檔本身也可以直接給人看。

---

## 以下貼給 Claude

我要接手淡江臺灣手語專題的語言模型端（中文→手語 Gloss）。請先讀
`Unsloth與腳本格式評估_2026-08-20.md` 與 `NEXT_STEPS.md`，再依下面的任務進行。

### 環境

- 專案在 `/Users/leo/Documents/語言模型`，GitHub `manmanliang0622/tku-tsl-text-to-gloss`
- 目前分支 `video-coverage-eval`（今天的成果都在這，已 push）
- 學校 VM：`ssh -p 2288 b310ai@163.13.202.125`（金鑰已設，免密碼）
  - RTX 4060 Ti **16GB，是共用生產機**
  - 訓練環境有兩套：`~/tku-tsl-text-to-gloss/.venv`（自製管線）
    與 `~/unsloth-venv`（Unsloth，**兩者不可混用**）
  - VM 沒有 git 認證：VM 跑 → scp 回 Mac → Mac commit/push

### 現在正在跑什麼（2026-08-20 下午）

VM 上有一輪訓練進行中：`~/train_ctrl.log`（自製管線的控制對照，約 1 小時 15 分後完成）。
**跑完請依序做**：

1. 挑 dev loss 最佳 checkpoint，用
   `eval_json_model.py --adapter ~/outputs/qlora_e4b_v12c_ctrl/checkpoint-XXX
   --tag v13c --test-file test.jsonl --ple gpu` 評估
2. 把 `~/outputs/qlora_e4b_v12c_ctrl` 改名成 `qlora_e4b_v13c`
   （訓練中不能改，會動到正在寫入的目錄）
3. 接著跑 E2B 對照：模型在
   `~/.cache/huggingface/hub/models--google--gemma-4-E2B-it`（已下載），
   用 `scripts/train_unsloth.py --model <E2B路徑> --output ~/outputs/qlora_e4b_v13e2b`，
   **超參數不要改**（要與前兩輪可比），約 40 分鐘
4. 三輪都評估完後，**恢復 0813 服務**（現在是停的）：
   ```
   cd ~/0813 && setsid nohup .venv/bin/python3 bundle_server.py \
     --host 127.0.0.1 --port 8083 --model-port 8877 &
   ```
   驗證：`curl -s http://127.0.0.1:8083/health` 要 `status: ok`，
   再 `POST /translate {"text":"今天天氣很好"}` 應回 `今天/天氣/好`

### 三方對照要回答的問題

| 輪次 | 框架 | 模型 | 狀態 |
|---|---|---|---|
| v13c | 自製 train_qlora | E4B | 跑中 |
| v13u | Unsloth | E4B | 完成：BLEU 70.71／ROUGE-L 79.12／EM 66.67／ValidJSON 96.97 |
| v13e2b | Unsloth | E2B | 待跑 |

比 v13c vs v13u → 換不換框架。比 v13u vs v13e2b → 能不能用小模型。
已知速度：自製 5.95 秒/步、Unsloth 4.29 秒/步（快 39%）。

### 主線任務：教授指定的新資料格式

教授要求把訓練資料改成「從候選清單挑 sign_id」（`tsl-script-v1`），
缺必要手語時輸出 `needs_review=true`。**基礎建設已完成**：

- `scripts/build_sign_inventory.py` → `data/signs/sign_inventory.jsonl`（16,628 個 sign_id）
- `scripts/sign_candidates.py` → 候選檢索器（訓練／上線**必須共用同一支**）
- `scripts/build_script_dataset.py` → `data/splits_script/`（10,001 句，已產出）
- `scripts/eval_script_format.py` → 新格式的指標（約束違反率、needs_review P/R、可播放率）

**還沒做**：訓練腳本還沒接新格式。`train_unsloth.py` 目前讀舊的 `splits_json`
並透過 `prompt_common` 組 prompt；新格式的資料本身已是完整 `messages`
（system/user/assistant），不需也不能再過 `prompt_common`，要加資料格式開關。
另外 max_len 要從 512 調到 768（新格式 prompt 中位數 654 token），
顯存會超過現在的 11.1GB，可能要 batch 降到 1、grad_accum 加倍補回。

**⚠️ 但先別急著訓練**，有兩件事教授一裁示就會讓整批資料重生成：
1. **sign_id 命名**：目前流水號 `TSL_01084`；教授範例用語義代號 `TSL_TODAY`，
   但 16,628 個詞無法全取英文名。建議流水號為主＋高頻詞語義別名。
   **ID 一旦寫進訓練資料就不可改**。
2. **候選序列化**：教授原格式 k=25 就要 688 token（超過現行 max_len 512）；
   壓成 `TSL_01084=今天` 省 36%，語意相同。建議採壓縮版並把 k 提到 40。

### 人工評估／校訂資料怎麼併回去（重要，順序容易搞錯）

有兩份人工成果，**性質不同、用法也不同**：

**A. v11 盲測評分表（100 題）** — `outputs/人工評估表_v11_盲測.xlsx`
目前 repo 裡這份是**空白範本**，評分者填好的版本**尚未上傳**，要先跟團隊拿。
拿到後注意：**評分（1–5 分）不是訓練資料**，不能拿「這句得 2 分」去做 SFT。
它的用途有三個：①計畫 6.2 要求的人工評估佐證，寫報告用；
②【建議正確 Gloss】欄若有填，那才是可用的黃金修正；
③日後若做 RL/DPO，評分可當偏好訊號。

**B. 文化部語料校訂（5,125 筆，已大致完成）** —
`/Users/leo/Documents/人工評估/人工校訂_文化部臺灣手語語料庫_5125筆.xlsx`
主審判定：通過 4,580／需更正 527／排除 18，其中 421 筆填了更正 Gloss。
**這份才是直接影響訓練的**——它修的是訓練語料本身的錯標。

⚠️ **陷阱**：該檔的「最終判定／最終Gloss／可匯入訓練」是 **Excel 公式欄，
從未重算過**，用 openpyxl 讀 cached value 全是空的。不要直接讀那幾欄，
要嘛用 Excel 開啟重算後另存，要嘛在 Python 裡依「主審判定＋更正Gloss」自行推導。

**併回去的正確順序（這是問題的答案）**：
不要「先訓練舊格式、再改新格式」，也不要在訓練階段注入修正。
修正屬於**來源資料層**，格式是衍生產物：

```
① 修正併進 data/splits 來源（走既有 scripts/add_correction.py／corrections 機制）
② 重新產生兩種下游格式：
     build_json_targets.py    → data/splits_json    （舊格式）
     build_script_dataset.py  → data/splits_script  （新格式）
③ 才訓練
```

理由：`build_script_dataset.py` 讀的就是 `data/splits/*.jsonl`，
來源修好、兩種格式自動繼承。若在訓練階段注入，換格式時得整套重做一次。
而且既然要換新格式，**就不必再訓練舊格式**，省一輪 GPU。

`data/splits/*.jsonl` 有 gitignore（可由 `split_data.py` 再生），
入庫的是 `manifest.json`，所以修正要進 corrections 機制而不是直接改 jsonl。

### 目前最大的瓶頸（不是模型問題）

新格式的 dev 詞涵蓋率卡在 79.1%，缺的**不是影片素材**（庫裡真沒有的只剩 18 個
token），而是語義距離遠的對應：「我學會了→領悟」「味道都沒變→一樣」
「訴說→講述」。字面比對與共現統計都撈不到，模型再怎麼微調也選不到不在候選裡的詞。
要突破需語義向量檢索，但「共用機不裝額外依賴」是既有原則，需先確認可否放寬。

### 版號與命名

今天的訓練輪叫 **v13**（v13c／v13u／v13e2b），**刻意跳過 v12**——
v12 已被既有的「上下文（前文）模型」佔用。線上服務維持 **v11**，
因為 v12 的上下文實驗實測只有雜訊等級提升且疑問類型退步 3.6pp
（`NEXT_STEPS.md` 已列「已證實無效」）。若教授再問上下文，可回覆：
測過了但我們的實作與 SCOPE 論文有一處差異——SCOPE 的前文是**與輸出同語言**，
我們 v12 給的是前一句中文，應給前一句 Gloss，該變體尚未驗證。

### 一定要知道的坑

1. **Gemma 4 的 chat template 標記是 `<|turn>model\n`**，不是 Gemma 3 的
   `<start_of_turn>model\n`。用錯 `train_on_responses_only` 不會報錯，
   只會靜默退回全序列 loss。日誌要看到「train_on_responses_only 已套用」。
2. **訓練與評估的 prompt 必須逐字一致**。今天因為自組 prompt 與
   `eval_json_model.py` 不同，33 句 test 的 EM 與 ValidJSON 全部掛零，
   看起來像模型壞掉，其實模型是好的。換框架時最該檢查這個。
3. **清顯存要停 0813 服務**：`serve_model.py` 是 `bundle_server.py` 的子程序，
   砍掉會自動重生（上限 3 次），要連砍到 health 顯示
   `model_restart_exhausted: true`。恢復必須重啟 bundle_server 本身。
4. **台語兩個服務也吃 GPU**：STT 是 systemd `stt-service`（需 sudo）；
   TTS 是 Docker `fatchord-wavernn_hapsing_1`（有 autoheal，
   **pkill 沒用要 `docker stop`**）。⚠️ `pkill -f "gunicorn.*suisiann"` 會匹配到
   ssh 指令字串本身而砍掉自己的遠端 shell，pattern 要寫 `[s]uisiann`。
   目前兩者都是停的狀態，**不要刪除**（TTS 對外開在 0.0.0.0:5000，是別人的服務）。
5. **推論一律 `device_map={"":0}`**，含 cpu 項目會慢 580 倍（35 秒/token）。
6. **33 句 test 太小**（僅 5 個 reference 4-gram），BLEU-4 會大幅跳動，
   以 EM／ROUGE-L 為主指標並註明變異。

### 誠實邊界（對外報告要守住）

- 涵蓋率是「候選能不能拼出參考答案」，不等於翻譯品質。
- gloss 層可驗證 ≠ 母語者確認自然道地；NMS／手形／地區變體屬影片軌。
- 三方對照只在**同一份資料、同一組超參**下才成立，跨版本比較要註明條件差異。

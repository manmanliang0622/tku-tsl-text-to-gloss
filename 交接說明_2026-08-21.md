# 交接說明（貼給 Claude 用）— 2026-08-21

> 用法：把「以下貼給 Claude」整段複製貼上，Claude 就有足夠脈絡接手。
> 本檔本身也可以直接給人看。
> 取代 `交接說明_2026-08-20.md`（那份已過時，勿再使用）。

---

## 以下貼給 Claude

我要接手淡江臺灣手語專題的語言模型端（中文→手語 Gloss）。請先讀
`Unsloth與腳本格式評估_2026-08-20.md` 與 `NEXT_STEPS.md`，再依下面的任務進行。

### 環境

- 專案在 `/Users/leo/Documents/語言模型`，GitHub `manmanliang0622/tku-tsl-text-to-gloss`
- 目前分支 `video-coverage-eval`，已 push 到 `09c9083`
- 學校 VM：`ssh -p 2288 b310ai@163.13.202.125`（金鑰已設，免密碼）
  - RTX 4060 Ti **16GB，是共用生產機**
  - 訓練環境兩套：`~/tku-tsl-text-to-gloss/.venv`（自製管線）
    與 `~/unsloth-venv`（Unsloth，**兩者不可混用**）
  - VM 沒有 git 認證：VM 跑 → scp 回 Mac → Mac commit/push（`git pull` 可以）
  - ⚠️ **VM 的 checkout 停在 `model` 分支、落後 21 個 commit，還有 4 個檔未提交修改。
    不要在 VM 上做 checkout／pull --force**，會蓋掉別人的工作。需要新版腳本就
    個別 scp 過去（先 md5 比對，相同就不用傳）。

### 目前狀態：新格式的技術路徑已全部打通，只等教授裁示

**唯一的卡點是教授對 sign_id 命名與候選序列化的裁示。ID 一旦寫進訓練資料
就不可改**，所以在裁示之前不要產生正式訓練資料。除此之外沒有技術未知了。

#### 已完成（2026-08-20～21）

**1. 三方對照結束，結論已定**

| 輪次 | 框架／模型 | 訓練時間 | 33 句 test | 167 句 corpus test |
|---|---|---|---|---|
| v13c | 自製 QLoRA／E4B | 114 分 | EM 69.7／ROUGE-L 83.28 | ROUGE-L 50.77／TokenF1 0.546 |
| v13u | Unsloth／E4B | 80 分 | EM 66.67／ROUGE-L 79.12 | ROUGE-L 49.23／TokenF1 0.528 |
| v13e2b | Unsloth／E2B | 57 分 | EM 57.58／ROUGE-L 74.91 | ROUGE-L 47.69／TokenF1 0.508 |

- **換框架：換。** v13c 與 v13u 在 167 句上互有勝負、差距都在 1.5pp 內，
  品質實質相當但訓練快 30%。33 句上看起來自製贏 3pp，但那組只有 5 個
  reference 4-gram，不能當判準。
- **小模型：這輪不要。** E2B 兩組測試一致落後（33 句 EM −9.1pp、
  167 句 TokenF1 −0.038），且 epoch 1 就最佳、epoch 2 過擬合，
  超參是為 E4B 調的。它的價值在解鎖 RL 微調（9GB），留給新格式那條路。
- 結果在 VM `~/tku-tsl-text-to-gloss/results/summary_v13*.json`，**尚未 scp 回 Mac**。
- ⚠️ **這些數字是在校訂前的舊測試集上算的**，不可與之後的直接比較（見下）。

**2. 人工校訂已套進來源語料**（`scripts/apply_corpus_review.py`，可重跑、已驗證冪等）

三份校訂表在 `data/reviews/`（已入版控，輸入可重現）：

| | 通過 | 取代 Gloss | 排除 | 其他 |
|---|---|---|---|---|
| 文化部 5,272 | 4,722 | 506 | 23 | 21 註記類 |
| 中正例句 544 | 534 | 10 | 0 | — |
| 合成句 967 | 921 | 45 | 0 | 1 未填 |

修在**來源層**（`parallel.jsonl`／`twtsl_sentences.jsonl`／`tsl_synth.jsonl`），
下游重跑就繼承。切分與兩種格式都已重生：

- train 8,992→**8,915**／dev 666→**663**／test_corpus 167→**166**
- dev 詞涵蓋率 79.09%→**79.33%**、test_corpus 整句可拼出 15.57%→**18.07%**

三個當時的判斷，日後有人問起要說得出來：

- **21 筆引號標註沒有採用。** 內容是「依語料庫原始標註補上「」」，
  但全庫 6,783 筆 Gloss 原本全形標點出現 0 次，照收會生出 `怎麼辦？」`
  這種對不到影片的 token。已去標點還原，理由存在 `review_note` 可翻案。
  其中 TC00251／TC02022 是「1/3」被斜線切開——**這份表以 `/` 為分隔符，
  本來就表達不了**，待另行處理。
- **合成句的追溯欄位分兩層。** 那 45 筆全落在 2026-07-24 老師已改過的句子上，
  `pre_review_gloss_text` 被那輪佔用，本輪另存 `review_baseline_gloss_text`。
- **`split_data.py` 原本不排除任何句子**，加了 `train_eligible` 閘門那 23 筆才
  踢得掉（`--keep-excluded` 可重現舊切分）。

**3. sign 總表清理完成，語義 ID 可以凍結了**

- `gloss` 欄維持動作庫原鍵不動（join key 兼 ID 錨點），清理值另存 `gloss_clean`
- `name_key`＝語義 ID 的命名基礎，碰撞加 `_2`
- 原本「16,628 筆 gloss 完全唯一」是假象——去掉控制字元與空白後冒出 7 組碰撞。
  已逐組比對動作資料查清：**4 組 frames sha256 完全相同**（訪談／許久／傾聽／
  抹黑，同一支錄影匯入兩次），標 `duplicate_of`；3 組是真變體。
- 「事情」兩支比過品質，選定 `TSL_00883`（文化部），`TSL_00004` 標 `superseded_by`
- **對外報告引用 `distinct_signs` 16,624，不是 16,628**
- 三組真變體都已比過品質並選定，**7 組全部結案**：
  事情 `TSL_00883` 勝 `TSL_00004`／共同 `TSL_02059` 勝 `TSL_02060`／
  軟弱 `TSL_14281` 勝 `TSL_14282`。**三組的敗方都是 moe 教育部辭典**，
  與既有影片品質盤點一致（該來源 92.7% 不佳）。共同與軟弱的 moe 版是
  `severe`（act_eff 0.070／0.035，幾乎整段偵測不到手），差一個數量級。
  敗方標 `superseded_by` 排出候選，ID 不刪。
- 候選池 16,621（扣 4 重複 + 3 取代）
- ⚠️ 日後若有**新的**碰撞出現，`build_sign_inventory.py` 會標「真變體·未比品質」
  提醒，比法見下方「影片品質怎麼比」。

**4. 新格式訓練已 smoke test 通過**（12 步實跑，非紙上推估）

| | 流水號 `TSL_01084=今天` | 語義 ID `TSL_今天` |
|---|---|---|
| 序列中位數 | 631 token | **377 token** |
| 秒/步 | 13.11 | **8.18** |
| 峰值顯存 | 12,636 MiB | **11,978 MiB** |
| 2 epochs 全跑 | 4 小時 10 分 | **2 小時 36 分** |

- **顯存夠，超參不必動**。舊交接文件擔心的「batch 降到 1、grad_accum 加倍」
  是不必要的，維持 batch 2 × grad_accum 8 即可，與 v13 各輪可比。
- **遮罩正確**：日誌要看到 `train_on_responses_only 已套用（Gemma 4 <|turn> 標記）`
  與 `遮罩檢查：122/695 token 參與 loss`。
- 兩次都是 max_len 768（腳本強制），差距純粹來自實際序列長度；
  語義版最長樣本只有 424 token，max_len 可退回 512。
- ⚠️ smoke 的 eval_loss（0.274 vs 0.312）**不能比品質**——12 步且 tokenization 不同。
- VM `~/outputs/smoke_script`、`~/outputs/smoke_sem` 是拋棄式產物，已放
  「請勿使用」標記。**`smoke_sem` 的 sign_id 是未凍結的拋棄式語義 ID，
  切勿評估或上線。**

### 待教授裁示（兩題，材料都已備妥）

**① sign_id 命名 → 建議中文語義 ID `TSL_<gloss>`**

教授範例用 `TSL_TODAY`。原本擋住這條路的理由是「16,628 個詞無法全取英文名」——
用中文就沒有這個限制。而且實測顯示流水號**反而最貴**，因為 Gemma 4 把數字逐位切開：

```
TSL_01084  → ['TS','L','_','0','1','0','8','4']   8 token
TSL_TODAY  → ['TS','L','_','TOD','AY']            5 token
TSL_今天    → ['TS','L','_','今天']                4 token
```

流水號看不懂，候選一定得補 `=今天` 才有語意 → 10 token。而且 assistant 輸出的
`sign_ids` 陣列用的是同一批 ID，**輸入輸出兩邊都在付這個代價**。

給教授的說法：照您指定的語義代號做，用中文而非英文；實測同一份資料、同一組超參下，
每輪訓練從 **4 小時 10 分降到 2 小時 36 分**，顯存從 12.6GB 降到 12.0GB。

**② 候選序列化 → 建議壓縮版**

dev 真實候選 k=40 的 user 訊息中位數：教授原格式（物件＋流水號）**868**／
壓縮 `TSL_01084=今天` **507**／語義 ID `TSL_今天` **267**／純 gloss 147。
語義 ID 版連 `=gloss` 都不需要（ID 自己就說明了自己）。
省下的長度可把 k 從 40 提到 60，dev 涵蓋率 79.9%→82.9%。

### 裁示下來之後的動作

1. 改 `build_sign_inventory.py` 產語義 ID（`name_key` 已備妥，碰撞規則已定）
2. 重跑 `build_script_dataset.py` 產正式訓練資料
3. `train_unsloth.py --format script`，超參維持不動，約 2 小時 36 分
4. 用 `scripts/eval_script_format.py` 評估（約束違反率、needs_review P/R、可播放率）

### 已經試過、不要再走的路

- **候選同義展開通道**（`sign_candidates` 的 `n_syn`，預設 0＝關閉）：
  實測是**淨負的**——train 詞涵蓋率 92.6%→90.7%，dev 只有 +0.1pp。
  固定 k 之下候選是零和的，塞同義詞就得擠掉別的。種子放寬到例句／對齊通道
  再掉 0.7pp。k=60 時轉為 +0.22pp，**k 拉高後值得重測，現在不要開**。
  同義詞組表本身仍有價值，在 `data/signs/synonym_groups.json`（1,733 組）。
- **v12 上下文（前文）模型**：實測只有雜訊等級提升且疑問類型退步 3.6pp。
  若教授再問，可回覆：測過了，但我們的實作與 SCOPE 論文有一處差異——
  SCOPE 的前文是**與輸出同語言**，我們 v12 給的是前一句中文，應給前一句 Gloss，
  該變體尚未驗證。

### 目前最大的瓶頸（不是模型問題）

新格式的 dev 詞涵蓋率 79.3%，缺的**不是影片素材**（庫裡真沒有的只剩 18 個 token），
而是語義距離遠的對應：「我學會了→領悟」「味道都沒變→一樣」「訴說→講述」。
字面比對、共現統計、同義詞表**都撈不到**（同義詞表已試，見上）。
要突破需語義向量檢索，但「共用機不裝額外依賴」是既有原則，需先確認可否放寬。

### 影片品質怎麼比（共同／軟弱還沒做）

同名不同錄影要選哪一支時，**不要用肉眼看，用既有的品質掃描**：
VM `~/0813/quality_scan/entries_final.csv`，關鍵欄位——

- `n_raised_L/R` vs `n_frames`：舉手幀佔比。比例低代表片段裡一大半是手放下的空檔，
  當單詞片段播出就是好幾秒沒動作（「事情」的 moe 版 6.83 秒裡只有一半舉手）
- `wrist_vis_L/R`：手腕可見度。**缺 landmark 是 retarget 端補不出來的**
- `jit_max`：抖動；`act_eff`：較差側的 act_rate；`usage`：線上實際使用次數
- `tier` 只有三級，同為 poor 時要看上面那些欄位才分得出高下

判斷「是重複收錄還是真變體」則比動作資料本身：
`~/0813/recordings/<recording>.json` 的 `frames` 欄位算 sha256，相同就是同一支。

### 版號與命名

訓練輪 **v13**（v13c／v13u／v13e2b），**刻意跳過 v12**——v12 已被上下文模型佔用。
線上服務維持 **v11**。新格式那輪還沒編號。

### 一定要知道的坑

1. **Gemma 4 的 chat template 標記是 `<|turn>model\n`**，不是 Gemma 3 的
   `<start_of_turn>model\n`。用錯 `train_on_responses_only` 不會報錯，
   只會靜默退回全序列 loss。日誌要看到「train_on_responses_only 已套用」。
2. **訓練與評估的 prompt 必須逐字一致**。曾因自組 prompt 與 `eval_json_model.py`
   不同，33 句 test 的 EM 與 ValidJSON 全部掛零，看起來像模型壞掉，其實模型是好的。
3. **清顯存要停 0813**：`serve_model.py` 是 `bundle_server.py` 的子程序，
   單獨砍會自動重生（上限 3 次）。正確做法是**先砍父程序 `bundle_server`、
   再砍 `serve_model`**。恢復必須重啟 `bundle_server` 本身：
   ```
   ssh -p 2288 b310ai@163.13.202.125 'cd ~/0813 && setsid nohup .venv/bin/python3 \
     bundle_server.py --host 127.0.0.1 --port 8083 --model-port 8877 \
     > ~/0813_restore.log 2>&1 < /dev/null &'
   ```
   驗證 `curl -s http://127.0.0.1:8083/health` 要 `status: ok`，
   再 `POST /translate {"text":"今天天氣很好"}` 應回 `今天/天氣/好`。
4. ⚠️ **`pgrep -f` / `pkill -f` 會匹配到 ssh 指令字串自己**。
   2026-08-21 實際踩到：`pgrep -f "bundle_server..."` 把自己的遠端 shell 砍了。
   **pattern 一律用中括號**：`[b]undle_server`、`[s]erve_model`、`[s]uisiann`。
   （舊文件只警告了台語服務的 `pkill`，其實停 0813 也一樣會中。）
5. **台語兩個服務也吃 GPU**：STT 是 systemd `stt-service`（需 sudo）；
   TTS 是 Docker `fatchord-wavernn_hapsing_1`（有 autoheal，**pkill 沒用要 `docker stop`**）。
   目前兩者都是停的，**不要刪除**（TTS 對外開在 0.0.0.0:5000，是別人的服務）。
6. **推論一律 `device_map={"":0}`**，含 cpu 項目會慢 580 倍（35 秒/token）。
7. **33 句 test 太小**（僅 5 個 reference 4-gram），BLEU-4 會大幅跳動，
   以 EM／ROUGE-L 為主指標並註明變異。
8. **在 VM 上啟動長時程／常駐程序時，ssh 連線常常不會返回**（即使用了
   `setsid nohup ... > log 2>&1 < /dev/null &`，實測仍會卡到逾時）。
   程序其實**有正常啟動**——不要因為 ssh 逾時就重試，那會啟動第二份。
   正確做法：讓那條 ssh 逾時或放背景，然後**另開一條 ssh 連線**用
   `pgrep -af "[t]rain_unsloth"` 或 `curl /health` 確認實際狀態。

### 誠實邊界（對外報告要守住）

- 涵蓋率是「候選能不能拼出參考答案」，**不等於翻譯品質**。
- gloss 層可驗證 ≠ 母語者確認自然道地；NMS／手形／地區變體屬影片軌。
- 2026-08-21 的人工校訂是**訓練資料層**的審查（中文語意＋Gloss 詞彙與語序），
  **不是模型輸出的人工評估**。計畫 6.2 要求的是後者，v11 盲測評分表已取消，
  **這個缺口目前沒有來源**，要嘛之後補做，要嘛在報告裡據實說明沒做。
- 三方對照只在**同一份資料、同一組超參**下才成立。v13 那三輪是在**校訂前的
  舊測試集**上評估的（test_corpus 已換過一批，ids sha256 已變），
  要與之後比較必須先重跑評估。
- 切分是照 MODEL_CARD／NEXT_STEPS 記載的正式配方跑的，**不含 `--context 2`**；
  硬碟上原本那份帶著 v12 上下文實驗的殘留。要保留上下文加 `--context 2` 重跑。
- 動作庫「有幾個手語」請用 16,624（`distinct_signs`），16,628 含已驗證的重複收錄。

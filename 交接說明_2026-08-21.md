# 交接說明（貼給 Claude 用）— 2026-08-21

> 用法：把「以下貼給 Claude」整段複製貼上，Claude 就有足夠脈絡接手。
> 本檔本身也可以直接給人看。
> 取代 `交接說明_2026-08-20.md`（那份已過時，勿再使用）。

---

## 以下貼給 Claude

我要接手淡江臺灣手語專題的語言模型端（中文→手語 Gloss）。請先讀
`Unsloth與腳本格式評估_2026-08-20.md` 與 `NEXT_STEPS.md`，再依下面的任務進行。

### 環境

- 專案在 `/Users/leo/Documents/語言模型`，GitHub `manmanliang0622/tku-tsl-text-to-gloss`
- 目前分支 `video-coverage-eval`，已 push 到 `09c9083`
- 學校 VM：`ssh -p 2288 b310ai@163.13.202.125`（金鑰已設，免密碼）
  - RTX 4060 Ti **16GB，是共用生產機**
  - 訓練環境兩套：`~/tku-tsl-text-to-gloss/.venv`（自製管線）
    與 `~/unsloth-venv`（Unsloth，**兩者不可混用**）
  - VM 沒有 git 認證：VM 跑 → scp 回 Mac → Mac commit/push（`git pull` 可以）
  - ⚠️ **VM 的 checkout 停在 `model` 分支、落後 21 個 commit，還有 4 個檔未提交修改。
    不要在 VM 上做 checkout／pull --force**，會蓋掉別人的工作。需要新版腳本就
    個別 scp 過去（先 md5 比對，相同就不用傳）。

### 目前狀態：教授已裁示，訓練資料已產好，下一步就是訓練

2026-08-21 教授同意兩項建議：**中文語義 ID `TSL_<gloss>`** 與**壓縮候選格式**。
遷移已完成，`data/splits_script/` 是語義 ID 版，可以直接訓練。

#### 已完成（2026-08-20～21）

**1. 三方對照結束，結論已定**

| 輪次 | 框架／模型 | 訓練時間 | 33 句 test | 167 句 corpus test |
|---|---|---|---|---|
| v13c | 自製 QLoRA／E4B | 114 分 | EM 69.7／ROUGE-L 83.28 | ROUGE-L 50.77／TokenF1 0.546 |
| v13u | Unsloth／E4B | 80 分 | EM 66.67／ROUGE-L 79.12 | ROUGE-L 49.23／TokenF1 0.528 |
| v13e2b | Unsloth／E2B | 57 分 | EM 57.58／ROUGE-L 74.91 | ROUGE-L 47.69／TokenF1 0.508 |

- **換框架：換。** v13c 與 v13u 在 167 句上互有勝負、差距都在 1.5pp 內，
  品質實質相當但訓練快 30%。33 句上看起來自製贏 3pp，但那組只有 5 個
  reference 4-gram，不能當判準。
- **小模型：這輪不要。** E2B 兩組測試一致落後（33 句 EM −9.1pp、
  167 句 TokenF1 −0.038），且 epoch 1 就最佳、epoch 2 過擬合，
  超參是為 E4B 調的。它的價值在解鎖 RL 微調（9GB），留給新格式那條路。
- 結果在 VM `~/tku-tsl-text-to-gloss/results/summary_v13*.json`，**尚未 scp 回 Mac**。
- ⚠️ **這些數字是在校訂前的舊測試集上算的**，不可與之後的直接比較（見下）。

**2. 人工校訂已套進來源語料**（`scripts/apply_corpus_review.py`，可重跑、已驗證冪等）

三份校訂表在 `data/reviews/`（已入版控，輸入可重現）：

| | 通過 | 取代 Gloss | 排除 | 其他 |
|---|---|---|---|---|
| 文化部 5,272 | 4,722 | 506 | 23 | 21 註記類 |
| 中正例句 544 | 534 | 10 | 0 | — |
| 合成句 967 | 921 | 45 | 0 | 1 未填 |

修在**來源層**（`parallel.jsonl`／`twtsl_sentences.jsonl`／`tsl_synth.jsonl`），
下游重跑就繼承。切分與兩種格式都已重生：

- train 8,992→**8,915**／dev 666→**663**／test_corpus 167→**166**
- dev 詞涵蓋率 79.09%→**79.33%**、test_corpus 整句可拼出 15.57%→**18.07%**

三個當時的判斷，日後有人問起要說得出來：

- **21 筆引號標註沒有採用。** 內容是「依語料庫原始標註補上「」」，
  但全庫 6,783 筆 Gloss 原本全形標點出現 0 次，照收會生出 `怎麼辦？」`
  這種對不到影片的 token。已去標點還原，理由存在 `review_note` 可翻案。
  其中 TC00251／TC02022 是「1/3」被斜線切開——**這份表以 `/` 為分隔符，
  本來就表達不了**，待另行處理。
- **合成句的追溯欄位分兩層。** 那 45 筆全落在 2026-07-24 老師已改過的句子上，
  `pre_review_gloss_text` 被那輪佔用，本輪另存 `review_baseline_gloss_text`。
- **`split_data.py` 原本不排除任何句子**，加了 `train_eligible` 閘門那 23 筆才
  踢得掉（`--keep-excluded` 可重現舊切分）。

**3. sign 總表清理完成，語義 ID 可以凍結了**

- `gloss` 欄維持動作庫原鍵不動（join key 兼 ID 錨點），清理值另存 `gloss_clean`
- `name_key`＝語義 ID 的命名基礎，碰撞加 `_2`
- 原本「16,628 筆 gloss 完全唯一」是假象——去掉控制字元與空白後冒出 7 組碰撞。
  已逐組比對動作資料查清（見下方「動作庫更新會讓舊判定失效」）。
- 「事情」兩支比過品質，選定 `TSL_00883`（文化部），`TSL_00004` 標 `superseded_by`
- **對外報告引用 `distinct_signs` 17,075，不是 17,078**
- **8 組碰撞全部有結論**（動作庫更新後從 7 組變 8 組）：
  重複收錄 3（訪談／許久／抹黑）；已選定 4（事情／共同／軟弱／求救）；
  **兩支皆不堪用 1（傾聽）**。敗方標 `superseded_by` 排出候選，ID 不刪。
- **傾聽兩支都是 severe**（moc_G2C27_R act_eff 0.2、moe_05_0022 act_eff 0.0），
  選哪支都演不出來。列在 `BOTH_UNUSABLE`，屬**素材缺口要換源重錄**，不是命名問題。
- 已選定的四組中，敗方三組是 moe 教育部辭典、一組是 moe——與既有影片品質盤點
  一致（該來源 92.7% 不佳）。
- 候選池 17,074（扣 3 重複 + 4 取代，取代者不進候選）
- ⚠️ 日後若有**新的**碰撞出現，`build_sign_inventory.py` 會標「真變體·未比品質」
  提醒，比法見下方「影片品質怎麼比」。

**4. 新格式訓練已 smoke test 通過**（12 步實跑，非紙上推估）

| | 流水號 `TSL_01084=今天` | 語義 ID `TSL_今天` |
|---|---|---|
| 序列中位數 | 631 token | **377 token** |
| 秒/步 | 13.11 | **8.18** |
| 峰值顯存 | 12,636 MiB | **11,978 MiB** |
| 2 epochs 全跑 | 4 小時 10 分 | **2 小時 36 分** |

- **顯存夠，超參不必動**。舊交接文件擔心的「batch 降到 1、grad_accum 加倍」
  是不必要的，維持 batch 2 × grad_accum 8 即可，與 v13 各輪可比。
- **遮罩正確**：日誌要看到 `train_on_responses_only 已套用（Gemma 4 <|turn> 標記）`
  與 `遮罩檢查：122/695 token 參與 loss`。
- 兩次都是 max_len 768（腳本強制），差距純粹來自實際序列長度；
  語義版最長樣本只有 424 token，max_len 可退回 512。
- ⚠️ smoke 的 eval_loss（0.274 vs 0.312）**不能比品質**——12 步且 tokenization 不同。
- VM `~/outputs/smoke_script`、`~/outputs/smoke_sem` 是拋棄式產物，已放
  「請勿使用」標記。**`smoke_sem` 的 sign_id 是未凍結的拋棄式語義 ID，
  切勿評估或上線。**

### 教授已裁示（2026-08-21）：中文語義 ID ＋ 壓縮候選

兩項都同意。遷移已完成，`data/splits_script/` 已是語義 ID 版。

**為什麼是中文而不是英文**：教授範例用 `TSL_TODAY`，原本擋住的理由是
「16,628 個詞無法全取英文名」——中文沒有這個限制。而且 Gemma 4 把數字逐位切開，
流水號反而最貴：`TSL_01084` 8 token／`TSL_TODAY` 5／`TSL_今天` 4，
且流水號看不懂所以候選一定得附「=今天」（10 token）。

**實際產出的效果**（dev，Gemma 4 tokenizer 實測）：

| | 流水號 | 語義 ID |
|---|---|---|
| 整段序列中位數 | 631 | **380**（最大 426） |
| 秒/步 | 13.11 | **8.18** |
| 峰值顯存 | 12,636 MiB | **11,978 MiB** |
| 2 epochs 全跑 | 4 小時 10 分 | **2 小時 36 分** |

`max_len` 512 就夠（`train_unsloth.py` 目前對 script 格式強制拉到 768，
是安全餘裕，不影響速度——padding 是動態的）。

⚠️ 遷移時順手修掉一個冗餘：`--compact` 原本無條件輸出 `TSL_現在=現在`，
但語義 ID 自己就是詞面，gloss 被寫了兩次。已改為偵測 `inventory_stats.json`
的 `id_scheme`，語義方案只輸出 sign_id（中位數 472→380）。

`--id-scheme serial` 可逐筆重現舊流水號（實測 `ids_changed 0`），
每列也留了 `legacy_sign_id`，舊資料要重跑不會斷。

### ⚠️ 動作庫更新會讓舊判定失效（2026-08-21 實際踩到）

`data/video/lexicon.json` 是 VM `~/0813/recordings/lexicon.json` 的鏡像，**會過期**。
8/21 更新時從 16,628 長到 17,078（純新增零移除，主要是臺圖詞庫 slw 247 筆）。

問題在於：**動作庫會把同一個 gloss 改指到別支錄影**。「傾聽」原本兩支
frames sha256 相同、判為重複收錄，更新後正本改指 `moc_G2C27_R`，
兩支變成完全不同的錄影——沿用舊判定就會把一個真正不同的手語當成重複刪掉。

已加防呆：`DUPLICATE_OF`／`SUPERSEDED_BY` 的值是
`(正本 gloss, 本筆當初驗的 recording, 正本當初驗的 recording)`，
建表時比對現況，**對不上直接中止並列出差異**（已實測會擋）。
更新動作庫後若看到這個中止訊息，**要重驗，不要改數字讓它過**。

判定表以 **gloss（動作庫原鍵）為鍵**而非 sign_id——sign_id 的形式會隨命名方案改變，
gloss 才是跨方案穩定的錨點。

### 測試集組成（2026-08-22 換過一個）

| 切分 | 句數 | 來源 | 用途 |
|---|---|---|---|
| test | 33 | 自有標記真實句 | 歷史對照。問候語為主的短句（參考平均 2.39 詞），不代表整體能力 |
| test_corpus | 166 | 文化部語料庫留存 | **對外報告主指標**，依對話群組整組留存 |
| **test_textbook** | **423** | **台灣手語教材** | 2026-08-22 起取代論文例句 |
| ~~test_papers~~ | ~~143~~ | 中正大學論文例句 | **已停用**（來源檔仍在，加 `--papers-as-test` 可叫回） |

**台灣手語教材**（`scripts/build_textbook_testset.py` → `data/tsl_textbook/`）：
來源 https://tslcopus.deaf.com.tw/ 「臺灣手語教材-搜尋網」，教材編製
Jung-Hsing Chang。交付時檔名寫「教育部手語資料」但**不是教育部的**，
與 `data/moe/`（教育部常用手語辭典）是完全不同的來源，不可混引。

⚠️ **授權未查證**：該站首頁與列表頁均無版權宣告。與文化部／中正（已查證
訓練＋散布皆合法）不同。比照 `data/moe/` 保守處理——`data/tsl_textbook/`
已在 .gitignore，可本機／VM 評估，**論文附錄不附原始句對**。要公開先去信
tsl@deaf.com.tw。

原始 429 列 → 423 句：去重 4、排除 2 句與既有語料中文重複者（測試洩漏）。
正規化清掉全形標點與裝飾空格（該檔用 ` / ` 當分隔符，既有語料則否）。
洩漏檢查中文 0／句對 0；Gloss 單獨碰撞 1 筆（`美麗`）**只報不斷言**——
教材句 Gloss 中位數 4 詞、最短 1 詞，單 token 必然碰撞，那是巧合非記憶。

### 涵蓋率現況（語義 ID ＋ 17,078 筆動作庫）

| 切分 | 詞涵蓋率 | 整句可拼出 | 庫裡真沒有 |
|---|---|---|---|
| train 8,915 | 92.6% | 75.2% | 416 |
| dev 663 | **79.5%** | 50.8% | 14 |
| test 33 | **98.7%** | 97.0% | **0** |
| test_corpus 166 | 69.7% | 18.1% | 9 |
| **test_textbook 423** | **78.7%** | **44.0%** | **172** |

⚠️ 教材集有 **172 個 token 是動作庫裡真的沒有的**（佔它缺口的 41%，
對照 test_corpus 只有 9/382＝2.4%）。缺的是「島上／走遍世界各地／
聽障奧林匹克運動會／技擊類運動／調香師」這類具體名詞與長片語——
**這是補片待辦的訊號，不是模型問題**。

### 0821：新格式的服務實例（2026-08-21 建立）

新格式模型輸出的是 sign_id 不是 gloss 字串，解析與播放路徑不同，
混進 0813 會把線上的 v11 弄壞，故另開 `~/0821`。**0813 完全不動。**

- `recordings`／`.venv`／`quality_scan`／`model_service/base_model` 是 **symlink**
  指回 0813（共 77GB），0821 實佔只有 48MB
- 程式、`models/`、`vendor/`、`signavatar-core/`、`model_service/scripts` 是複本
- 埠 **8084 / 8878**（0813 是 8083 / 8877）
- ⚠️ **0813 與 0821 不能同時服務**：0813 佔 9,400 MiB、v14 佔 9,722 MiB，
  合計超過 16,380 MiB。唯一的省記憶體選項 `--ple cpu` 是慢 580 倍的已知坑。
  要展示 v14 就得先停 0813，反之亦然。
- 2026-08-22 用它跑完整個 423 句評估（`/tmp/eval_via_service.py`）：連續 423 次
  請求 `model_restart_count` 維持 0，零錯誤零空輸出。**評估可以直接打線上服務，
  不必另外載第二份模型**——前提是 `sign_inventory.jsonl` 雜湊相同且
  `train.jsonl` 的 `(chinese, gloss_text)` 序列一致（檢索器只讀這兩欄），
  否則候選會不同、評估不成立。這兩項每次都要先驗。
  服務端的候選外過濾在 423 句裡實際擋掉 18 個 ID——那道保險不是裝飾。
- 2026-08-21 已實際啟動驗證通過：`/health` 的 `model_identity_ok: true`、
  `/translate` 出 gloss＋sign_ids、`/clips` 取得動作幀。延遲 4.6–6.1 秒/句
  （比 v11 慢，多了候選檢索與較長的 prompt）。
- ⚠️ **複製服務目錄時不要只挑程式副檔名**。當初用
  `cp *.py *.js *.html *.json *.txt *.md` 漏掉了 `style.css`（44KB）與
  五張 `.webp`，結果 HTML 有出來但完全沒樣式，看起來就像「UI 不見」。
  已補齊並逐一 HTTP 驗證。要再開一份服務時，用
  `python3 /tmp/sweep.py` 那類掃描比對 HTML/JS 引用的檔案是否都在。
- 前端仍是 v11 的 UI（吃 `gloss` 欄位，已保留相容），**畫面上看不到
  `sign_ids` 與 `needs_review`**，要看得直接打 API。
- ⚠️ 已確認 `bundle_server` 對 recordings 只讀不寫（寫入只去 clip_cache 與 logs），
  但**匯入素材的腳本會寫 recordings，不要從 0821 跑那些**，否則等於改到 0813
- ⚠️ 0813 若刪掉或搬移 recordings／.venv，0821 會一起壞
- 細節見 VM 上的 `~/0821/README_0821.md`

### 已經試過、不要再走的路

- **候選同義展開通道**（`sign_candidates` 的 `n_syn`，預設 0＝關閉）：
  實測是**淨負的**——train 詞涵蓋率 92.6%→90.7%，dev 只有 +0.1pp。
  固定 k 之下候選是零和的，塞同義詞就得擠掉別的。種子放寬到例句／對齊通道
  再掉 0.7pp。k=60 時轉為 +0.22pp，**k 拉高後值得重測，現在不要開**。
  同義詞組表本身仍有價值，在 `data/signs/synonym_groups.json`（1,733 組）。
- **v12 上下文（前文）模型**：實測只有雜訊等級提升且疑問類型退步 3.6pp。
  若教授再問，可回覆：測過了，但我們的實作與 SCOPE 論文有一處差異——
  SCOPE 的前文是**與輸出同語言**，我們 v12 給的是前一句中文，應給前一句 Gloss，
  該變體尚未驗證。

### v14（新格式首輪）已完成，結果與三個必須守住的解讀

訓練 2 小時 35 分 47 秒（推估 2h36m）、峰值顯存 11,806 MiB、8.16 秒/步。
`BEST_EPOCH=1`（eval_loss 0.1957；epoch 2 升到 0.2325 已過擬合），
**用 `checkpoint-558`**，不是最後那個。adapter 在 VM `~/outputs/qlora_e4b_v14script/`。

| | test 33 | test_corpus 166 | test_papers 143 |
|---|---|---|---|
| ValidJSON | 100% | 100% | 100% |
| 約束違反（列／ID） | 0%／0% | 4.22%／0.70% | 0.7%／0.18% |
| 可播放率 | 100% | 99.5% | 100% |
| needs_review P／R | — | 0.941／**0.118** | 1.000／**0.119** |
| BLEU-4／ROUGE-L／EM | 78.96／89.39／72.7% | 25.92／63.43／7.2% | 23.58／67.55／18.9% |

**① BLEU 有兩組，別引用錯（2026-08-21 已修，`eval_script_format.py` 同時輸出）。**
`ref_sign_ids` 只含候選撈得到的詞，撈不到的移到 `oov_items` 不在參考裡。

| | 候選內參考 | **完整參考** | 參考被拿掉 |
|---|---|---|---|
| test 33 | 78.96／89.39／72.7% | 77.80／88.38／69.7% | 1.3% |
| test_corpus 166 | 25.92／63.43／7.2% | **16.20／52.33／2.4%** | 30.3% |
| test_textbook 423 | 28.17／66.83／17.3% | **22.99／60.88／14.9%** | 21.3% |
| ~~test_papers 143~~ | 23.58／67.55／18.9% | 18.39／60.80／13.3% | 25.0% |

（BLEU-4／ROUGE-L／EM。test_papers 已停用，數字留作換血前後的難度對照——
教材集 22.99／60.88／14.9 與論文集 18.39／60.80／13.3 相當，換血沒讓測試變鬆。）

- **候選內參考**回答「有沒有從候選裡挑對」＝模型能力，數值天生偏高
- **完整參考**才是含檢索缺口的系統整體水準，**對外報告用這組**

有了完整參考才能跟 v11 對照（同一個任務定義）：

| | v11（線上，舊格式） | v14（新格式，完整參考） |
|---|---|---|
| test_corpus BLEU／ROUGE-L／EM | 14.83／50.08／1.20% | 16.20／52.33／2.41% |
| **test_corpus 可播放率** | **66.96%** | **99.5%** |
| test_papers 可播放率 | 78.20% | **100%** |

**新格式真正的突破是可播放率 66.96%→99.5%，不是 BLEU（只小贏）。**
⚠️ 但這個對照有兩個限制必須註明：test_corpus 換過一批（167→166，校訂前後），
且 v14 的訓練資料含 561 筆人工校訂——**部分增益可能來自資料品質而非格式**，
要嚴格歸因得再跑控制變因。

**② needs_review recall 原本只有 11.8%——已用機率校準修好，未重訓。**

    test_corpus       precision  recall   F1
    貪婪 true/false      0.941    0.118   0.209
    校準（門檻 0.02）     0.952    0.728   0.825   ← 現行
    全部都 flag（退化）    0.819    1.000   0.901

recall 提升 6.2 倍而 precision 沒付代價。做法：推論時多存 `p_needs_review`
（`needs_review` 那個位置 true/false 兩者間正規化的機率），再套門檻。
**門檻 0.02 是在 dev 250 句上依事前講定的規則選的**——「recall≥0.7 下最大化
precision」。ROC AUC：dev 0.7398、test_corpus 0.8286，訊號確實轉移。

**校準的泛化有限度（2026-08-22 用教材集獨立驗證）**：

| | 正例率 | precision | recall | F1 |
|---|---|---|---|---|
| dev（選門檻用） | 49.6% | 0.679 | 0.734 | 0.705 |
| test_corpus | 81.9% | 0.952 | 0.728 | 0.825 |
| **test_textbook** | 56.0% | **0.708** | **0.574** | **0.634** |

門檻在 dev 選定後轉移到 test_corpus 很成功，到教材集**只是堪用**——recall
比另外兩個集合都低。原先猜「應該落在 dev 與 test_corpus 之間」是錯的。
按 Gloss 長度拆也解釋不了（1–3 詞 0.698／4–6 詞 0.496／7+ 詞 0.679，非單調），
訊號分離本身就弱（該複審的 p 中位 0.0260 vs 不該的 0.0110，只差兩倍多；
test_corpus 上是 0.166 vs 0.049）。
**結論：校準有效但不是萬靈丹，換一批分布不同的資料就會退化。**
寫報告時務必標明門檻是在哪個集合上調的。仍遠勝貪婪（F1 0.634 vs 0.226）。

⚠️ 兩個解讀陷阱：
- 「全部都 flag」的 F1 0.901 比校準的 0.825 高，但那是**退化分類器**——它的
  precision 0.819 就等於 test_corpus 的正例率，零資訊量。F1 在 82% 正例的
  資料上會獎勵這種東西。正確的讀法是 precision 0.952 遠高於基準率 0.819；
  在平衡的 dev（49.6% 正例）上校準 F1 0.705 也贏過全部 flag 的 0.663。
- 事後診斷發現 test_corpus 上 F1 最佳門檻是 0.008（F1 0.921），**刻意不採用**：
  那是拿測試集調參。要重選門檻請重跑 dev 推論並沿用同一條規則。

原始成因（仍然存在，只是被校準繞過了）是訓練分布不匹配，不是模型笨：

    train        needs_review 標記率 24.8%（詞涵蓋率 92.6%）
    test_corpus  needs_review 標記率 81.9%（詞涵蓋率 69.7%）

模型學到 train 的先驗。而且這個落差是**結構性的**——候選檢索的例句遷移與
詞對齊表都由 train 建成，即使留一法，train 仍然比較容易。校準只是把門檻挪到
對的位置；若日後重訓，把 `needs_review` 的 loss 加權仍是值得一試的正攻法
（那一輪可順便把 `--lora-dropout 0` 當變因一起測，不必額外付 GPU）。

**③ 語義 ID 讓幻覺變容易，這是換方案時沒預料到的代價。**
7 個候選外的 ID 中有 5 個是總表也查無的：`TSL_身份`／`TSL_舉`／`TSL_五十分鐘`／
`TSL_扭`／`TSL_選手`。用中文當 ID 等於模型可以直接「造」一個看起來合理的；
換成流水號得掰數字，難得多。影響不大（可播放率 99.5%），
下游驗「ID 是否在候選內」即可全擋，但這條 trade-off 要記著。

### 下一步

1. ~~修指標的誠實性~~ **已於 2026-08-21 完成**：`eval_script_format.py` 現在
   同時輸出兩組（見上）。完整參考由 `data/splits/<split>.jsonl` 依 id 接回，
   該檔有 gitignore，缺檔時會印警告並略過那組——需先跑 `split_data.py` 再生。
2. ~~處理 needs_review recall~~ **已於 2026-08-21 用機率校準修好**（見上）。
   若要進一步改善，正攻法是重訓時加權 `needs_review` 的 loss，
   可與 `--lora-dropout 0` 一起當變因測。
3. adapter 放 `~/0821/model_service/checkpoint/`，並把 `bundle_server`
   的解析路徑改成吃 sign_id（目前仍是 v11 的 gloss 版）。

### 目前最大的瓶頸（不是模型問題）

新格式的 dev 詞涵蓋率 79.5%，缺的**不是影片素材**（庫裡真沒有的只剩 14 個 token），
而是語義距離遠的對應：「我學會了→領悟」「味道都沒變→一樣」「訴說→講述」。
字面比對、共現統計、同義詞表**都撈不到**（同義詞表已試，見上）。
要突破需語義向量檢索，但「共用機不裝額外依賴」是既有原則，需先確認可否放寬。

### 影片品質怎麼比

同名不同錄影要選哪一支時，**不要用肉眼看，用既有的品質掃描**：
VM `~/0813/quality_scan/entries_final.csv`，關鍵欄位——

- `n_raised_L/R` vs `n_frames`：舉手幀佔比。比例低代表片段裡一大半是手放下的空檔，
  當單詞片段播出就是好幾秒沒動作（「事情」的 moe 版 6.83 秒裡只有一半舉手）
- `wrist_vis_L/R`：手腕可見度。**缺 landmark 是 retarget 端補不出來的**
- `jit_max`：抖動；`act_eff`：較差側的 act_rate；`usage`：線上實際使用次數
- `tier` 只有三級，同為 poor 時要看上面那些欄位才分得出高下

判斷「是重複收錄還是真變體」則比動作資料本身：
`~/0813/recordings/<recording>.json` 的 `frames` 欄位算 sha256，相同就是同一支。

### 版號與命名

訓練輪 **v13**（v13c／v13u／v13e2b），**刻意跳過 v12**——v12 已被上下文模型佔用。
線上服務維持 **v11**。新格式那輪還沒編號。

### 一定要知道的坑

1. **Gemma 4 的 chat template 標記是 `<|turn>model\n`**，不是 Gemma 3 的
   `<start_of_turn>model\n`。用錯 `train_on_responses_only` 不會報錯，
   只會靜默退回全序列 loss。日誌要看到「train_on_responses_only 已套用」。
2. **訓練與評估的 prompt 必須逐字一致**。曾因自組 prompt 與 `eval_json_model.py`
   不同，33 句 test 的 EM 與 ValidJSON 全部掛零，看起來像模型壞掉，其實模型是好的。
3. **清顯存要停 0813**：`serve_model.py` 是 `bundle_server.py` 的子程序，
   單獨砍會自動重生（上限 3 次）。正確做法是**先砍父程序 `bundle_server`、
   再砍 `serve_model`**。恢復必須重啟 `bundle_server` 本身：
   ```
   ssh -p 2288 b310ai@163.13.202.125 'cd ~/0813 && setsid nohup .venv/bin/python3 \
     bundle_server.py --host 127.0.0.1 --port 8083 --model-port 8877 \
     > ~/0813_restore.log 2>&1 < /dev/null &'
   ```
   驗證 `curl -s http://127.0.0.1:8083/health` 要 `status: ok`，
   再 `POST /translate {"text":"今天天氣很好"}` 應回 `今天/天氣/好`。
4. ⚠️ **`pgrep -f` / `pkill -f` 會匹配到 ssh 指令字串自己**。
   2026-08-21 實際踩到：`pgrep -f "bundle_server..."` 把自己的遠端 shell 砍了。
   **pattern 一律用中括號**：`[b]undle_server`、`[s]erve_model`、`[s]uisiann`。
   （舊文件只警告了台語服務的 `pkill`，其實停 0813 也一樣會中。）
5. **這台機器上還有別人的 GPU 服務，都不要刪除**：
   - 台語 STT：systemd `stt-service`（需 sudo）。2026-08-21 查是停的。
   - 台語 TTS：Docker `fatchord-wavernn_hapsing_1`（有 autoheal，
     **pkill 沒用要 `docker stop`**）。2026-08-21 查該容器不在執行中，
     只剩 autoheal／hokbu-nginx／nginx-cache 三個周邊容器活著。
     TTS 對外開在 0.0.0.0:5000。
   - ⚠️ **`ollama` 容器（已跑 3 週）**——舊文件從未提及。平時 `ollama ps` 是空的、
     不佔顯存，**但只要有人送一個請求它就會把模型載進 GPU**。訓練佔 12GB 時
     只剩 4GB 餘裕，很可能被它直接 OOM 掉。這是別人的服務，不該停；
     長時間訓練若無故 OOM，先查 `docker exec ollama ollama ps`。
6. **推論一律 `device_map={"":0}`**，含 cpu 項目會慢 580 倍（35 秒/token）。
7. **33 句 test 太小**（僅 5 個 reference 4-gram），BLEU-4 會大幅跳動，
   以 EM／ROUGE-L 為主指標並註明變異。
8. **新格式推論的兩個環境坑（2026-08-21 實測）**：
   - `unsloth-venv` 裡**不能**用 `transformers` + `PeftModel` 載 adapter：
     `torchao` 是 0.12.0，peft 的分派器要求 >0.16.0，直接 ImportError。
     要用 `FastModel.from_pretrained(model_name=<adapter 目錄>)`，
     與訓練同一條路徑，順便繞開版本衝突。
   - **Gemma 4 的 tokenizer 是多模態 processor**，第一個位置參數是 `images`。
     `tok(prompt, ...)` 會讓 prompt 被當成圖片、text 變 None 而 TypeError。
     必須寫 `tok(text=prompt, ...)`。
   兩者都不是模型問題，是環境與 API 的坑，與坑 2 的 prompt 不一致同一類。
9. **在 VM 上啟動長時程／常駐程序時，ssh 連線常常不會返回**（即使用了
   `setsid nohup ... > log 2>&1 < /dev/null &`，實測仍會卡到逾時）。
   程序其實**有正常啟動**——不要因為 ssh 逾時就重試，那會啟動第二份。
   正確做法：讓那條 ssh 逾時或放背景，然後**另開一條 ssh 連線**用
   `pgrep -af "[t]rain_unsloth"` 或 `curl /health` 確認實際狀態。

### 誠實邊界（對外報告要守住）

- 涵蓋率是「候選能不能拼出參考答案」，**不等於翻譯品質**。
- gloss 層可驗證 ≠ 母語者確認自然道地；NMS／手形／地區變體屬影片軌。
- 2026-08-21 的人工校訂是**訓練資料層**的審查（中文語意＋Gloss 詞彙與語序），
  **不是模型輸出的人工評估**。計畫 6.2 要求的是後者，v11 盲測評分表已取消，
  **這個缺口目前沒有來源**，要嘛之後補做，要嘛在報告裡據實說明沒做。
- 三方對照只在**同一份資料、同一組超參**下才成立。v13 那三輪是在**校訂前的
  舊測試集**上評估的（test_corpus 已換過一批，ids sha256 已變），
  要與之後比較必須先重跑評估。
- 切分是照 MODEL_CARD／NEXT_STEPS 記載的正式配方跑的，**不含 `--context 2`**；
  硬碟上原本那份帶著 v12 上下文實驗的殘留。要保留上下文加 `--context 2` 重跑。
- 動作庫「有幾個手語」請用 17,075（`distinct_signs`），17,078 含已驗證的重複收錄。

# 交接說明 2026-09-02：v18 資料已就緒，等一個指令開訓

## 一句話

v18 訓練資料建好並驗收完畢，VM 端全部備妥、冒煙測試通過。**只差「停掉線上模型」
這一步**——它會讓對外翻譯功能暫停數小時，我沒有自行執行。

---

## 你要跑的三行

```bash
# 1) 上鎖，讓看門狗不再把模型載回 GPU（前端與 /clips 不受影響）
ssh tku-gpu 'touch ~/tsl-autopublish/training.lock'

# 2) 停掉目前佔卡的線上模型（翻譯功能自此暫停）
ssh tku-gpu 'pkill -f bundle_server; pkill -f "0821_bundle.*serve_model"'

# 3) 開跑（訓練→推論→選門檻→評分，一條龍，約數小時）
ssh tku-gpu 'nohup ~/run_v18.sh > /dev/null 2>&1 &'
```

看進度：`ssh tku-gpu 'tail -f ~/v18_run.log'`

**跑完會自動移除鎖**，看門狗 2 分鐘內把完整服務接回，tunnel 網址不變。
腳本有 `trap`，失敗或被中斷也會解鎖。

> `~/run_v18.sh` **不會自己動線上服務**。它只檢查顯存是否 ≥11500 MiB，
> 不足就直接退出並提示。訓練峰值需 ~11 GB，而線上模型佔 9.4 GB，
> 16 GB 的卡容不下兩者——這是記憶裡早就記過的既定事實，沒有繞路。

---

## 為什麼訓練與推論必須串在同一支腳本

2026-08-23 踩過：訓練一結束顯存釋放，看門狗 2 分鐘內就把線上模型載回 GPU；
接著才發車的推論只搶到殘渣、被 offload 到 CPU，**331 秒/句（慢 70 倍）**。
所以兩者之間不能有 GPU 空窗，`run_v18.sh` 因此把四步串成一條龍。

---

## ⚠️ 驗收時最重要的一件事：v18 的數字會比 v17 難看

這是**修正的結果，不是退步**。

| | v17（線上） | v18（待訓練） |
|---|---:|---:|
| 訓練列數 | 8,915 | 9,064 |
| schema | tsl-script-v1 | tsl-script-v3 |
| 平均候選數 | 40.0 | 58.9 |
| 平均目標 sign 數 | 5.31 | **4.61** |
| 候選覆蓋風險 | 25.9% | **58.0%** |

覆蓋風險翻倍、目標變短，是因為 v17 的候選有**標籤洩漏**——對齊表與核心詞
是用完整 train 建的，替 train 句產生候選時表裡already含有那句自己的答案。
移除洩漏後，train 詞涵蓋率從虛報的 92.5% 回到真實的 75.3%（**17.2 個百分點**，
是教授抽 20 筆估的 4.76pp 的四倍）。

**報告時務必標明口徑**，否則並排看會像做壞了。dev/test 不受影響——它們本來
就不在 train 裡，用完整 train 建的表看不到自己的答案。

---

## v18 與 v17 的差異總表

| 面向 | v17 | v18 | 出處 |
|---|---|---|---|
| 切分 | 只比原始字串 | 表面形式正規化（NFKC＋標點＋異體字） | 審查 2.4 |
| | 核心 33 有 3 句、dev 有 11 句與 train 重疊 | 重疊 0 | |
| 候選建表 | 完整 train（有洩漏） | leave-one-group-out | 審查 2.2 |
| 候選數 k | 40 | 60 | 重測 token 成本後 |
| 語義通道 | `n_sem=8`（線上載不起來） | `n_sem=0`（與線上一致） | 審查 2.1 |
| 候選池 | 含影片演不出動作的 | 排除 1,969 個 | 審查 4.1 |
| schema | v1 | v3（+compounds/reduplicated） | 審查 3.1 |
| 旗標欄位 | `needs_review` | `candidate_coverage_risk` | 審查 4.2 |
| `clause_breaks` | 8,915 列全空 | 100 列有真實邊界 | 審查 3.2 |
| 影片庫 | 8/30 掃描 | 9/02 重掃（621 支換源） | — |

候選參數存證在 `data/splits_script_v18/candidate_config.json`，
服務端啟動時會比對，不一致就拒絕啟動。

---

## 檔案最新狀態排查

`python3 scripts/staleness_audit.py` → **全部最新**（2026-09-02 02:5x）。

它掃的是整條鏈：影片庫／品質掃描 → 總表 → 切分 → 訓練資料 → 推論 → 指標。
過期不會拋錯，只會安靜地讓人用舊結論做新決定，所以做成可重複執行的稽核。

其他驗收：

- `python3 tests/run_all.py` → 11 支全過
- `python3 scripts/data_preflight.py` → 無阻斷性問題
- `python3 scripts/check_bundle_deps.py` → 部署相依宣告與程式一致
- CI（GitHub Actions）→ 連續綠

---

## 訓練完成後我還沒做的

`run_v18.sh` 只到「產出指標檔」。以下要人看過再決定：

1. **與 v17 逐項比較**——測試集位元相同故可直接比，但要標明 v17 的 train
   側數字是在洩漏下量的
2. **要不要部署 v18**——部署需要：checkpoint、新門檻、
   `candidate_config.json`、以及 `serve_model.DEPLOYED_SCHEMA` 改成 V3
3. **`BUNDLE_MODULES` 的檔案要一起帶**（`check_bundle_deps.py --deploy-check`）

---

## 我今晚犯的一個錯（已修復，但你該知道）

同步程式到 VM 時我在 `~/tku-tsl-text-to-gloss` 跑了 `git reset --hard`，
**把 VM 上四個檔案的未提交修改丟掉了，而且那個目錄當時在 `model` 分支上**。

- `model` 分支已依 reflog 還原到 `4d94f62`（我 reset 前的位置）
- `twtsl_sentences.jsonl`／`tsl_synth.jsonl` 與本機逐位元相同，沒有損失
- `parallel.jsonl` 的修改可由 `apply_corpus_review.py` 重跑重建（該腳本設計就是冪等）
- 之後改用獨立 worktree `~/tsl-v18`，不再碰任何既有分支

沒有做內容比對就 `reset --hard` 是我的疏失。

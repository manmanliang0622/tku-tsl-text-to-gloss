# 五份參考文獻 vs. 本專案訓練做法：對照檢視

日期：2026-08-12
方法：三篇論文 PDF 全文抽取後逐節比對（`pdftotext -layout`），ITRI 與 LinkedIn 以原文網頁比對；
程式面比對 `scripts/train_qlora.py`、`prompt_common.py`、`eval_json_model.py`、`metrics.py`。
原則：**論文寫了什麼**與**我的推論**分開標示。

---

## 0. 總結論

本專案的**工程嚴謹度高於三篇論文**（洩漏檢查、bootstrap CI、三層診斷都是論文沒做的），
但有 **3 處引用依據錯誤**、**4 項偏離機器翻譯正規做法**、**2 處對外結論措辭需修正**。

最重要的一件事：**SCOPE 的上下文增益是 Gloss→中文方向，本專案 v12 把它套到中文→Gloss 方向，
而且前文給錯了語言。** 這解釋了為什麼 v12 只拿到 +1.04 BLEU 而不是論文的 +3。

| 類別 | 項目數 | 嚴重度 |
|---|---:|---|
| A. 引用依據錯誤（結論被誤導） | 3 | 高 |
| B. 論文支持但沒做 | 2 | 中 |
| C. 偏離 MT 正規做法（便宜可修） | 4 | 中，其中 2 項幾乎零成本 |
| D. 對外措辭需修正 | 2 | 高（影響專題審查可信度） |
| E. 做得比論文好，不要改 | 4 | — |

---

## A. 引用依據錯誤

### A-1. ★ SCOPE 的 +3 BLEU 是 **Gloss→中文**，本專案是**中文→Gloss**，方向相反

**論文實際做的**（SCOPE, AAAI-25，Contextual LLM Fine-tuning 節）：

```
輸入：SLR 模組產生的 top-3 gloss 假設  +  前文的「口語文本」（最多前 3 句）
模型：Qwen2 + Q-LoRA
輸出：口語中文句子
```

也就是 **G2T（gloss→text）**。本專案的 T2G（text→gloss）是它的逆向任務。

三個連帶的誤植：

1. **前文的語言給錯了。** SCOPE 的前文是「前幾句的**口語文本**」，而它的輸出也是口語文本
   —— 前文與輸出**同語言**。對應到 T2G，同語言的前文應該是**前一句的 Gloss**。
   本專案 v12 給的是前一句**中文**，等於前文與輸出跨語言。
   → `NEXT_STEPS.md:87–88` 把「前文應給前一句 Gloss」標為「未驗證的假設」——
   **它其實就是 SCOPE 的原始設計，證據等級應從「假設」升為「論文做法」。**

2. **+3.5/+3.7 BLEU 那組數字不是上下文的功勞。** 論文在 Phoenix-2014T / CSL-Daily 上
   相對前人 SOTA 的 +3.73/+3.57、+3.50/+3.14 BLEU 是**整套方法**（含 embedding alignment
   encoder）的增益。真正歸因於上下文的是 SCOPE 自建資料集上的 **+3.26/+2.79 BLEU**，
   且論文原文寫的是「主要歸因於 context-aware LLM fine-tuning」。
   消融顯示移除 embedding alignment encoder 掉 **16.01 BLEU** —— 那才是主力元件，
   而它是**視覺端**模組，本專案（純文字）根本沒有對應物。

3. **上下文增益的前提是專為上下文而建的資料集**：59,231 句對話序列 / 72.4 小時 / 12 位聾人
   標註者。本專案 407 段 / 5,272 句、平均每段 13 句。

**該怎麼改**：`NEXT_STEPS.md` §2 第 1 項現在寫「已做，效果邊際，不建議再投入」。
這個結論**下得太早**——被測的是一個方向錯、語言也錯的變體。
建議把該節改為「已測版本（中文前文）無效；SCOPE 原始設計（**Gloss 前文**）尚未測」，
並列為一次低成本實驗（資料現成、`seg_uuid` 已分組、訓練 2h22m）。

### A-2. SignAlignLM **從未微調 T2G**，它的配方不能直接當 T2G 依據

論文明說：「G2T is the only DGS task we use for any fine-tuning」。
T2G 在 Table 5 是**零樣本／單樣本轉移任務**，而且是所有任務裡**分數最低**的：

| 任務 | Base LLaMA3 8B (BLEU-1) | 多任務 LLaMA3 8B |
|---|---:|---:|
| G2T（有微調） | 27.1（ft 模型） | 22.7 |
| **T2G**（未微調，1-shot） | **8.556** | **10.921** |
| T2I-G（0-shot） | 56.128 | 43.229 |

→ 計畫 §2 把 [1] 當作「T2G 任務定義」來源**正確**；當作「LoRA 微調配方先例」**也還可以**；
但**不能**當作「T2G 微調有效」的先例——論文沒做這件事。
本專案的 T2G 微調在文獻上其實**沒有直接先例**（SCOPE 是 G2T，CCL24 是純 ICL 不微調）。
這對專題報告是**加分**而非扣分：可以正當主張「T2G 的 LLM 微調本身即為本專案的貢獻點」。

### A-3. 「語法規則寫進 prompt」被論文實測否定，本專案的決策對、但理由記錯了

SignAlignLM Table 1（Base LLaMA3 8B, G2T）：

| 提示策略 | BLEU-1 | ROUGE-1 | BERTScore-F1 |
|---|---:|---:|---:|
| 0-shot | 24.5 | 0.277 | 0.841 |
| **規則提示（手語語法規則）** | **22.8** | **0.255** | 0.836 |
| 標記法提示 | 24.3 | 0.277 | 0.840 |
| **1-shot 範例** | **27.1** | **0.309** | **0.851** |

**規則提示比什麼都不給還差。** 論文的結論是「給規則不足以教會模型，給一個範例更有效」。

`scripts/prompt_common.py:30` 已設 `INCLUDE_RULES = False`，**決策是對的**，
但註解寫的理由是「規則佔 ~260 token、logits 記憶體」——純工程理由。
建議補上 SignAlignLM Table 1 這個實證依據，因為那才是更強的理由，
而且**同時影響 Stage A 基線的解讀**：Stage A 若量到「規則提示 < 0-shot」，
那不是本專案的規則寫壞了，而是**與論文一致的已知現象**。

---

## B. 論文明確支持、但本專案沒做

### B-1. Stage C 多任務混訓：計畫寫了、README/HANDOFF 都標 ⬜、程式裡沒有

`grep -rn "OpenOrca\|多任務\|混訓" scripts/` → **零命中**。
這是 SignAlignLM 的核心貢獻，也是計畫 §5 Stage C 的內容，至今未執行。

但要**誠實看待它的效果**，論文數據是雙面的：

| | G2T（微調目標任務） | T2G（本專案任務） | 通用能力（ARC/HellaSwag/WinoGrande） |
|---|---:|---:|---|
| 純手語微調 | **27.1** | 未報告 | 三項全退化（Figure 3） |
| 多任務混訓 | 22.7（**−4.4**） | **10.921**（vs base 8.556） | 退化大幅緩解 |

→ **在微調的目標任務上，多任務是掉分的**（−4.4 BLEU-1）。它換到的是不遺忘通用能力。
→ 在 T2G 上多任務優於未微調基線，但**論文沒有報告「純手語微調模型」在 T2G 的分數**，
   所以**不能推論**多任務在 T2G 上優於純微調。

**建議**：值得做，但要用對理由。不要期待它提升 EM/BLEU。
它要回答的是計畫 §5 Stage C 自己寫的那個問題——「模型上線後要面對使用者各式各樣的中文輸入
（含錯字、口語化表達）」。這是**穩健性**指標，需要另建測試（例如對 test_corpus 加入錯字、
口語化改寫後再測），而不是看現有指標。
混合比例：SignAlignLM 用**與手語資料相同的切分大小**（≈1:1，α≈0.5）。

### B-2. ITRI 的 RAG 是**兩段**，本專案只測了前半段就下了「RAG 無效」的結論

ITRI 文章原文描述 RAG 有兩個階段：
- **前處理**：把使用者文字轉 embedding，檢索相似範例。
- **後處理**：對照資料庫**修正生成錯誤**。

本專案 `scripts/rag_retrieve.py` 測的是前處理（檢索例句進 prompt），
結論「對未見句有害（EM 75.76%→69.70%）」。**後處理從未以 RAG 之名測過。**

有趣的是：**本專案的 `gloss_fallback.py` 就是 ITRI 的後處理 RAG**——
表外 Gloss 對照詞彙總表修正。所以本專案不是「沒做 RAG」，是**做了一半並且已經在跑**
（`serve_model.py` 預設開啟）。這需要改寫措辭，見 D-1。

---

## C. 偏離機器翻譯正規做法（可修，成本低）

### C-1. ★ 解碼用 greedy，沒有 beam search —— 最便宜的未採收益

```
eval_json_model.py:84   model.generate(..., do_sample=False)      # 無 num_beams
serve_model.py:129      model.generate(..., do_sample=False)
eval_model.py:108-109   model.generate(..., do_sample=False)
```

`do_sample=False` 且未指定 `num_beams` → **greedy 解碼**。
Beam search 是 NMT 的標準做法（BLEU 這套指標本身就是在 beam 解碼的前提下建立的）。
Gloss 序列短（多為 5–15 token），beam 5 的成本幾乎可忽略。

**這是唯一一項「改一個參數就可能拿到分數」的項目。** 建議先做 beam ∈ {1,4,5} 的 dev 掃描。
⚠️ 注意：`--target json` 的輸出是結構化 JSON，beam search 對格式遵循的影響需一併檢查
（有效 JSON 率目前 99–100%，不能因此掉下來）。

### C-2. 檢查點挑選用 dev **loss**，但計畫自己寫的是 dev **BLEU**

- 計畫 §5 Stage B：「以 dev 集 BLEU 做 early stopping 與 checkpoint 挑選」
- `MODEL_CARD.md:59`：「**選模**：僅依最低 dev loss」
- `train_qlora.py:231`：`load_best_model_at_end=False`，事後依 `trainer_state` 的 dev loss 挑

**計畫與實作不一致**，且實作偏離 MT 慣例。token-level cross-entropy 與序列層翻譯品質
不是單調關係——loss 較低的 checkpoint 未必 BLEU 較高。
好消息是每個 epoch 的 checkpoint 都保留了（`save_total_limit=None`），
**補跑 dev BLEU 不必重訓**，只要對現有 checkpoint 各跑一次 `eval_json_model.py --test-file dev.jsonl`。

### C-3. 每個 epoch 才存一次檢查點，2 epoch = 只有 2 個候選

`eval_strategy="epoch"` + `--epochs 2`，train 5,038 句 → 每 epoch 約 630 steps。
NEXT_STEPS §3 記載「各版皆 epoch 1–2 最佳，之後過擬合」——
在只有兩個觀測點的情況下，**最佳點很可能落在 epoch 1 之內**（例如 step 300–400）而沒被看到。
建議改 `save_strategy="steps"` / `save_steps=150`，配合 C-2 的 dev BLEU 選模。

### C-4. EM 是三篇論文都不用的指標，卻是本專案的主要敘事

| 論文 | 使用的指標 |
|---|---|
| SignAlignLM | BLEU-1/2、ROUGE-Lsum、BERTScore-F1 |
| SCOPE | BLEU-1~4、ROUGE-L（SLR 用 WER） |
| CCL24 | BLEU-1~4、ROUGE ＋ 5 分制人工 |
| **本專案** | **EM**、BLEU-4、ROUGE-L、可播放率 |

`NEXT_STEPS.md` §0 已經很好地說明「EM 理論上限約 23%」，但整份文件仍以 EM 為主軸敘事
（§1 表格第一個數值欄就是 EM、§2 各項也以 EM 論成敗）。
對外報告若以「EM 1.2%」開場，會讓不熟悉此任務的審查者判定專案失敗。
**建議把 BLEU-4 / ROUGE-L 提為主指標，EM 降為輔助診斷指標。** 理由見 D-2 的對照。

另補一項論文有、本專案沒有的：**BERTScore**（SignAlignLM 用）。
對「可理解的直譯 vs 母語化表達」這種語意接近但字面不同的情況，
BERTScore 比 BLEU 敏感，正好對應 NEXT_STEPS §2.3 舉的「氣象/宣布/昨天++」那個例子。
⚠️ 但中文 Gloss 沒有現成的合適 BERTScore 模型，且 Gloss 不是自然語言，需先驗證其有效性，
不要直接套用。

---

## D. 對外結論措辭需修正（影響專題可信度）

### D-1. 「RAG 無效」與兩篇參考文獻正面衝突，需限定範圍

`NEXT_STEPS.md:156` 把「RAG 檢索增強」列在「已證實無效，不要重做」。
但 ITRI [5] 與 CCL24 [2] 都以 RAG／知識庫為系統核心，CCL24 的「可能的改進方向」
最後一條還是「利用向量庫檢索技術，提供與輸入語句語義相近的上下文學習樣本」。

審查者看到這個矛盾會質疑。實際上兩者不衝突，因為適用對象不同：

| | 對象 | 本專案實測 |
|---|---|---|
| CCL24 的向量檢索 ICL | **未微調的商用 LLM**（文心4.0 等） | 不適用 |
| ITRI 的前處理 RAG | 檢索相似範例進 prompt | **已測，對未見句有害** |
| ITRI 的後處理 RAG | 對照資料庫修正生成錯誤 | **已做，就是 `gloss_fallback.py`** |

**建議改寫為**：「**對已微調模型**，以例句 ICL 形式的**前處理** RAG 無效
（EM 75.76%→69.70%）；此與 SignAlignLM『微調優於 ICL』一致。
ITRI 的**後處理** RAG（對照詞彙表修正）本專案以 `gloss_fallback.py` 實作，已預設啟用。」

### D-2. 「資料量遠小於 PHOENIX-14T」已經不成立

計畫 §8 風險 3 寫「即使合成後，規模仍遠小於 PHOENIX-14T、CSL-Daily 等基準資料集」。
實際規模對照：

| 資料集 | 訓練規模 | 詞彙 |
|---|---:|---:|
| CCL24（三個 CSL 語料庫合計） | **1,084 句**（其中 100 句作 ICL 樣本） | — |
| **本專案** | **5,038 句** | 兩層聯集 |
| PHOENIX-14T | ~7,000 句 | 3k（德語） |
| CSL-Daily | 20,654 | 2k |
| SCOPE | 59,231 序列 / 72.4h | 5k |

本專案與 PHOENIX-14T **同一量級**，且**比 CCL24 大近 5 倍**。
「遠小於 PHOENIX-14T」應刪除，改為「與 PHOENIX-14T 同量級，小於 CSL-Daily／SCOPE 一個數量級」。

這不只是措辭問題——它**支持三層診斷的結論**：
瓶頸不是句數不夠，是**詞彙覆蓋與領域廣度**（NEXT_STEPS §1.1 新第 1 優先）。
方向是對的，現在有了外部規模對照作為佐證。

---

## E. 做得比三篇論文更嚴謹，不要改

1. **資料洩漏檢查**：本專案逐項驗證群組／中文／Gloss／(中文,Gloss) 四種洩漏皆為 0，
   並保留 sidecar SHA-256。SCOPE 只有一句「carefully ensuring…」，CCL24 完全沒提。
   （SCOPE 有一點值得對齊：它明確要求「dev/test 的句子不得出現在訓練集的**上下文對話**中」——
   本專案 `test_corpus` 依對話群組整組留存，**已符合**，✓。）
2. **bootstrap 95% CI**：三篇論文都沒有報信賴區間。本專案有（以 group 為抽樣單位），
   這是 MT 論文的正規做法（Koehn 2004），比參考文獻做得好。
3. **三層診斷（Seen / Paraphrase / Unseen）**：三篇論文都沒有這種歸因設計。
   CCL24 只做了 train vs test 兩層對照。
4. **同批句子做 A/B**（三層診斷的 Seen 與 Paraphrase 用同一批 100 句）——正確的對照設計。

### E-補：CCL24 獨立佐證了本專案觀察到的落差

CCL24 文心4.0 在**同一批語料**上：

| | BLEU-1 | BLEU-4 | ROUGE |
|---|---:|---:|---:|
| Train（ICL 樣本內） | 96.30 | **91.22** | 96.67 |
| Test（未見） | 66.67 | **18.79** | 63.20 |

BLEU-4 從 91.22 掉到 18.79。本專案三層診斷 Seen 68.86 → Unseen 20.24（BLEU-4）。
**同樣的落差模式，未見層落點幾乎相同（18.79 vs 20.24）。**

這是很有力的外部驗證：**這個落差是 T2G 任務的常態，不是本專案的資料或方法出了問題。**
建議寫進 NEXT_STEPS §1.1 與對外報告。
⚠️ 但 CCL24 的 test 只評了 **10 句**（原文：「從訓練集和測試集中各選 10 句」），
統計上極不可靠，引用時必須標明樣本數，不可用來宣稱「本專案優於／持平 CCL24」。

---

## F. 兩個尚未採用、但有論文依據的技術點

### F-1. SCOPE 的 top-3 候選 → LLM 挑選（可改造為更有原則的 fallback）

SCOPE 把 SLR 的 **top-3 gloss 假設**一起餵給 LLM，讓它「summarize the top 3 glosses and
guess the correct words by checking previous texts」。

**〔我的推論，非論文結論〕** 本專案沒有 SLR 上游，不能直接照搬。
但它啟發一個比現行規則式 fallback 更有原則的設計：
用 beam search 產生 N 個候選 Gloss 序列（C-1 本來就要開 beam），
選「詞彙表內率最高」的候選輸出。這用模型自己的機率分布做選擇，
不像現行 fallback 是事後字串替換——也就不會踩到 `NEXT_STEPS.md` §6 記載的
「用測試集錯誤設計修復規則」那個坑。成本：只有解碼時間，不需重訓。

### F-2. SCOPE 的 Gloss 詞彙正規化：7k → 5k

SCOPE 用手語語言學規則（分詞與詞的組合）把 gloss 詞彙從 7k 降到 5k，
原文說這「顯著提升資料集品質」。

這對本專案「中正辭典 950 組別名中 448 組（47%）與語料庫衝突」的問題有直接啟示：
**正規化表要自建、以語料庫實際用法為準**，不是照搬辭典別名。
`NEXT_STEPS.md` §6「辭典與語料庫衝突時，以語料庫為準」**已經是對的**，
現在多了一個論文先例支持，可以在報告中引用。

---

## G. 一份參考文獻的品質警告

**[4] LinkedIn 貼文**是五份來源中唯一沒有同儕審查、沒有評估數據的。
它在計畫 §2 被用來佐證「面部表情是手語的語法成分」——
但這件事有真正的學術來源（Tai & Tsay 2015、Jane Tsay 2021，都已在 [7] 中），
**不需要靠一則社群貼文**。

更值得注意的是該貼文底下的留言批評：有人指出 LLM 層做的是
「機率性的重建而非翻譯」。這個批評**適用於本專案**——正好對應
NEXT_STEPS §2.3 記載的現象（模型輸出是可理解的直譯，不夠母語化）。

**建議**：對外報告移除 [4] 作為技術依據，或降級為「產業界關注度」的旁證。
專題審查時引用 LinkedIn 貼文會是可攻擊點。

---

## H. 修改清單（依「成本／收益」排序）

| # | 動作 | 成本 | 依據 |
|---|---|---|---|
| 1 | 開 beam search（dev 掃 beam 1/4/5，一併檢查 JSON 有效率） | 極低，不重訓 | C-1，MT 正規做法 |
| 2 | 改用 dev **BLEU** 選 checkpoint（現有 checkpoint 補跑即可） | 低，不重訓 | C-2，計畫 §5 自己寫的 |
| 3 | 改寫「RAG 無效」措辭；刪「遠小於 PHOENIX-14T」 | 極低 | D-1、D-2 |
| 4 | 主指標改 BLEU-4／ROUGE-L，EM 降為輔助 | 極低 | C-4 |
| 5 | 把 CCL24 的 91.22→18.79 落差寫進報告作外部佐證 | 極低 | E-補 |
| 6 | **重測上下文：前文改給前一句 Gloss** | 中（一次訓練 ~2h22m） | **A-1，SCOPE 原始設計** |
| 7 | `save_steps=150` + dev BLEU，找 epoch 1 內的最佳點 | 中（需重訓一次） | C-3 |
| 8 | Stage C 多任務混訓（α≈0.5），並另建「錯字／口語化」穩健性測試 | 高 | B-1；⚠️ 別期待 EM 提升 |
| 9 | beam N-best + 詞彙表內率挑選，取代規則式 fallback | 中 | F-1〔推論〕 |
| 10 | 人工評估加入「文化適應性」維度 | 低 | CCL24 的四維設計 |
| 11 | prompt_common.py 補上 INCLUDE_RULES=False 的實證依據 | 極低 | A-3 |
| 12 | 對外報告移除 [4] LinkedIn 作為技術依據 | 極低 | G |

**不建議做的**：擴大 LoRA 容量、再爬同源長句、爬中正辭典
（`NEXT_STEPS.md` §3 已有實測否證，五份文獻沒有任何一份與之衝突）。

---

## I. 人工評估：CCL24 的數據值得先看

CCL24 的自動指標不差（Test BLEU-1 66.67），但 5 分制人工評分是：

| 評審組 | 準確性 | 自然性 | 可讀性 | 文化適應性 |
|---|---:|---:|---:|---:|
| 專家組 | 2.10 | 2.25 | 2.21 | 2.07 |
| 採集組 | 2.14 | 2.07 | 1.96 | **1.11** |
| 普通組 | 2.93 | 2.89 | 2.89 | 2.57 |

**懂手語的人給 2 分出頭，不懂的人給 2.9。** 且該系統在 14 支隊伍中排名第 2
（14 支裡只有 4 支「能有效表達手語語義」）。

兩個推論：
1. **自動指標會大幅高估品質**，且愈是專業評審給分愈低。
   → `NEXT_STEPS.md` §2.3 把人工評估列第 3 優先，**應該提到第 1 或第 2**，
   工具（`make_human_eval_sheet.py`）已備妥，只差執行。
2. **「文化適應性」是所有維度中最低的**（採集組 1.11/5）。
   這正是本專案「可理解的直譯但不夠母語化」的同一個問題，
   建議把此維度加進 5 分制評分表（計畫 §6.2 目前是語意／語序／NMS／可播放性四維）。

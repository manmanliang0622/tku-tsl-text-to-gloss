---
title: 教授回饋對帳、資料解析修正與驗收排程（可查驗版）
date: 2026-08-13
purpose: 交給第三者逐項查驗。每一個數字都附「重現指令」，不附指令者一律標示為未驗證。
scope: 語言模型端（中文 → 臺灣手語 Gloss）。不含影片辨識、動作生成。
---

# 教授回饋對帳與 D1 進度（2026-08-13）

**驗收時間：下週。形式：現場 demo（模型要跑得起來）。**

本文件分四部分：

1. 教授四點回饋的逐項對帳（現況實測）
2. 今天實際改了什麼（含重現方式）
3. 尚未做、以及做不到的事（誠實界線）
4. 待決事項

查驗方式：所有指令都在 repo 根目錄執行。凡標 **〔已驗證〕** 者，本次會期內實際跑過並附輸出；標 **〔沿用未複驗〕** 者為前次會期結論，本次未重跑。

---

## 0. 先講清楚本專案的範圍

這是**整套手語系統中的一個翻譯模組**：輸入中文句子，輸出臺灣手語 Gloss 詞序。

**不是**手語影片辨識，**不是**手語動作生成，**不輸出也不保證** NMS（非手部標記：表情／搖頭／揚眉）、手形、地區變體。這些屬「影片軌」，需母語者看影片裁定。

整體管線與本 repo 的位置：

```
中文 → 【本 repo：翻譯 → TSL Gloss】 → 動作生成或動作庫檢索
     → 動作串接與平滑 → 表情/頭部/身體同步 → Avatar 骨架驅動 → 影片播放
```

此界線在 [MODEL_CARD.md](MODEL_CARD.md) 的「定位與界線」段落已有記載（2026-08-04），本次未變更，**但該卡的數字停在 v4，尚未更新到 v11／v12**（見第 3 節）。

---

## 1. 教授四點回饋 × 現況對帳

### 點 1：講清楚做的是翻譯模組，不要講得像完成整套系統

**現況：文件層面已做到，簡報層面待辦。**

| 檢查項 | 結果 |
|---|---|
| MODEL_CARD 是否標明界線 | ✅ 有「定位與界線（先讀）」段，明列「不是什麼」 |
| README 是否標明本 repo 只負責前兩段 | ✅ 有完整管線圖與「本 repo 負責前兩段」 |
| MODEL_CARD 數字是否為現況 | ❌ 停在 v4／2026-08-04，引用「584 句 BLEU 18.61」 |

〔已驗證〕`head -30 MODEL_CARD.md`、`head -12 README.md`

---

### 點 2：先回頭檢查訓練資料（替代用法／錯誤標記／方向與空間資訊被串成同一句）

**現況：教授說中了，而且問題比原先認知的大。已於今日修正。**

#### 2.1 病灶

[`scripts/extract_paper_examples.py`](scripts/extract_paper_examples.py) 的 `clean_gloss_line`（修正前第 68 行）：

```python
t = t.split("/")[0]   # 「/」在論文是替代詞 → 取第一個
```

註解的意圖正確，但這行在 **whitespace 切詞之後**才執行。論文排版時「/」兩側的空白不一致：

```
看/ *見/ *欣賞 心情      ← 「/」後有空白 → 舊版失效
難看/不漂亮/醜 1         ← 「/」後無空白 → 舊版有效
```

前者會讓 `*見`、`*欣賞` 各自變成獨立 Gloss **串進參考答案**，等於把論文標記為「不合語法」的寫法當成標準答案。

第二個坑：`*`（不合語法）緊貼標的（`*光了 1`），`?`（存疑）卻不緊貼（`? 光了 2`）。**只過濾「以 `*` 開頭的 token」會漏掉 `?` 標記的替代詞**——標記本身被當空 token 丟掉，被標記的詞卻活下來。

實例（修正前的參考答案）：

```
姊姊很怕蟑螂        姊姊/蟑螂/怕/可怕/很      「怕」與「可怕」是替代詞，不是兩個詞
我的耐心已經消磨殆盡  我/耐心/磨/耗盡/光了/*光了  「光了」來自 ? 標記的替代項
看心情！            看/*見/*欣賞/心情
```

#### 2.2 影響範圍〔已驗證〕

```bash
python3 scripts/reparse_paper_glosses.py
```

```
等價性驗證：296/296 筆可由 raw_gloss_line 重現
重算後有變動：43/296 筆，其中 test_papers 39/143
```

test_papers 那 39 句的組成：**33 句**參考答案含 `*`（token 級過濾就能修）＋ **7 句**是不帶標記的替代詞（只有 slot 級解析才修得到）－ **1 句**兩者皆有（「我的耐心已經消磨殆盡」）＝ 39。

**論文例句全部落在 test_papers，train／dev 各 0 筆**〔已驗證〕：

```bash
python3 -c "
import json, collections
for s in ['train','dev','test','test_corpus','test_papers']:
    rows=[json.loads(l) for l in open(f'data/splits/{s}.jsonl')]
    print(s, dict(collections.Counter(r['source'] for r in rows)))"
```

```
train        {'tslcorpus': 6817, 'twtsl-sentence': 929, 'synth': 1246}
dev          {'tslcorpus': 493, 'synth': 129, 'twtsl-sentence': 44}
test         {'real': 33}
test_corpus  {'tslcorpus': 167}
test_papers  {'paper': 143}
```

→ **本次修正只動測試集，不動訓練資料，因此不需重訓、也不需重跑推論。**

#### 2.3 一個必須寫進註解的限制：`*` 對、`?` 錯——這兩個符號在本專案不是同一件事

全切分掃描結果〔已驗證〕，**此為修正前的狀態**：

| 切分 | 含 `*` | 含 `?` | 來源 |
|---|---:|---:|---|
| train | 0 | 41 | 全為 tslcorpus |
| dev | 0 | 5 | 全為 tslcorpus |
| test_corpus | 0 | 2 | 全為 tslcorpus |
| test_papers | 33 | 0 | 全為 paper |

⚠️ 修正已套用，**現在重跑下方指令，test_papers 會是 0**（這正是修正生效的證據）；train／dev／test_corpus 的 41／5／2 應**維持不變**——若這三個數字有變動，代表規則被誤套到語料庫上，必須回退。

語料庫的 `?` 是**它自己的疑問句尾標記**，語意與論文的「存疑」完全不同：

```
你有帶傘嗎？           → 你/傘/帶來/有沒有?
你知道福爾摩沙是什麼意思嗎？ → 福爾摩沙/四個字/意思/什麼/你/知道?
```

另有 2 筆是**轉寫缺字**，性質又不同：`?觀/一整天`（參觀）、`心/?定`（安定）。

**若把修正寫成全域規則，會刪掉 48 筆語料庫的疑問標記**——而疑問類型正確率 93–99% 正是本模型少數跨測試集穩定習得的能力。此限制已寫入 `clean_gloss_line` 的 docstring。

重現：

```bash
python3 -c "
import json, collections
for s in ['train','dev','test','test_corpus','test_papers']:
    rows=[json.loads(l) for l in open(f'data/splits/{s}.jsonl')]
    st=[r for r in rows if '*' in r['gloss_text']]
    q=[r for r in rows if '?' in r['gloss_text']]
    print(s,'star',len(st),'q',len(q),
          collections.Counter(r['source'] for r in st+q))"
```

#### 2.4 教授點名的「方向和空間資訊」：是主動取捨，不是遺漏

論文的方向性標記（`弟弟 i 哥哥 j 打 2 (j→i)`）目前由 `NOTATION_RE`（`extract_paper_examples.py`）**整句排除**——296 句中 153 句因含 `i`/`j`/`→`/分類詞而不進資料集。

理由是下游動作庫無法檢索這種寫法。**但 JSON 目標 schema 裡有 `agreement` 欄位，並未從論文的 `(j→i)` 灌入。** 這是已知取捨，尚未處理，驗收時應主動說明而非迴避。

〔已驗證〕含 `→` 的論文例句 39 句、含 `i/j` 索引者更多，合計 153 句被 `has_notation` 標記排除。

#### 2.5 教授要的「找熟悉臺灣手語的人抽查」：抽查表已產出，尚未送出

解析修好只完成教授第 2 點的前半。後半是「再找熟悉台灣手語的人幫忙抽查，確認這些 Gloss 到底對不對」——**train 的 5,347 句從未經母語者逐句抽查過**。

⚠️ **這件事不能用計畫 6.2 的人工評估表交差**，兩者對象不同：

| | 對象 | 取樣來源 | 形式 |
|---|---|---|---|
| `make_human_eval_sheet.py` | **模型輸出**對不對 | 留存測試集 | 盲測 A/B |
| `make_data_audit_sheet.py` | **訓練資料本身**對不對 | `train` | 非盲測，直接判 |

新增 [`scripts/make_data_audit_sheet.py`](scripts/make_data_audit_sheet.py)，分層抽樣 150 句〔已驗證〕：

| 抽樣層 | 母體 | 抽樣 | 為什麼抽這層 |
|---|---:|---:|---|
| 語料庫·長度落差大 | 1,369 | 40 | 中文字數與 Gloss 詞數相差 ≥ 7，既有方法論標定的風險候選 |
| 語料庫·一般 | 2,647 | 40 | 同來源對照組，用來估整體錯誤率而非只看最可疑的 |
| 辭典例句 | 496 | 25 | 檢查是否為中文照抄 |
| 合成句·未審核 | 750 | 35 | `review_status=pending`，規則模板生成 |
| 合成句·已審核 | 85 | 10 | 信度對照組：老師若推翻自己 2026-07-24 審過的句子，代表標準要重新對齊 |

**一個必須主動講的數字**：先前對外說「合成句經手語老師 2026-07-24 審核」，實際上 train 內 835 句合成句只有 **85 句**有審核紀錄（79 句 `teacher-reviewed-2026-07-24` ＋ 6 句 `corpus-verified`），其餘 **750 句仍是 `pending`**。當時審的是 115 句「已有可具體指出問題」的高風險句，不是全部。

```bash
python3 scripts/make_data_audit_sheet.py
```

輸出 `outputs/訓練資料抽查表_train.xlsx`（150 題）與 `outputs/訓練資料抽查_抽樣紀錄.json`（抽樣參數與逐筆 id，供第三者重現與回填）。腳本只讀不寫，不改動任何資料。

**狀態：表已產出，尚未送出、尚未有任何人評過。**

---

### 點 3：測試方式要重新整理

#### 3.1 測試資料有沒有進訓練：沒有〔已驗證〕

```bash
python3 -c "
import json
def load(s): return [json.loads(l) for l in open(f'data/splits/{s}.jsonl')]
tr=load('train')
trzh={r['chinese'] for r in tr}
trpair={(r['chinese'],r['gloss_text']) for r in tr}
for s in ['dev','test','test_corpus','test_papers']:
    rows=load(s)
    print(s, '中文重疊', sum(1 for r in rows if r['chinese'] in trzh),
             '完全相同對', sum(1 for r in rows if (r['chinese'],r['gloss_text']) in trpair))"
```

```
dev          中文重疊 8    完全相同對 0
test         中文重疊 0    完全相同對 0
test_corpus  中文重疊 0    完全相同對 0
test_papers  中文重疊 0    完全相同對 0
```

三個測試集與訓練集**零重疊**。dev 有 8 句中文與 train 相同但 Gloss 不同（見 3.2 的標籤噪音），需另行處置。

#### 3.2 訓練資料筆數的正確說法〔已驗證〕

```
train 列數                8,992
相異 (中文, gloss) 對      5,347
其中複製而來的列           3,645（40.5%）
```

複製來自 `--length-balance` 的**刻意過取樣**（[`split_data.py`](scripts/split_data.py) 約第 405 行）：≤4 詞 ×1、5–7 詞 ×2、≥8 詞 ×4，目的是矯正模型輸出過短的偏差。

→ **對外一律寫「5,347 句、長度平衡後 8,992 列」，不可寫「8,992 句」。**

另有 **45 句中文對到多個不同 Gloss**（標籤噪音），例：

```
我要看報紙  →  報紙/我/欣賞/要   /   報紙/我/看/要
每天游泳對身體很好 → 每天/游泳/幫我/身體/好 / 每天/游泳/對/身體/棒
```

尚未決定是否合併。

#### 3.3 「不要一邊看測試結果一邊改規則」：曾經犯過，已自行抓出並移除

[NEXT_STEPS.md](NEXT_STEPS.md) 第 6 節記載：fallback 初版的「中文虛詞刪除表」是直接看未見層錯誤手工列出的，等於拿測試集調參，該規則已整條移除。〔沿用未複驗〕

**本次的解析修正刻意避開同一個陷阱**：修正依據是論文自己的標記規則（`*` 不合語法、`?` 存疑），不是分數。可佐證的是——若只做「丟掉 `*` 開頭的 token」這個簡單版本，分數與完整修正**幾乎完全相同**：

| 參考答案版本 | EM% | ROUGE-L | BLEU-4 |
|---|---:|---:|---:|
| 修正前（原樣） | 11.19 | 55.23 | 16.51 |
| 只丟 `*` 開頭的 token | 13.29 | 56.98 | 17.71 |
| **slot 級完整修正（已套用）** | **13.29** | **57.08** | **17.71** |

多修的那 7 句讓 EM 與 BLEU **完全不變**、ROUGE-L 只動 +0.10。**若以分數為判準，這 7 句根本不值得修**——修它們的唯一理由是那些參考答案在語言學上是錯的：

```
姊姊很怕蟑螂     只丟*: 姊姊/蟑螂/怕/可怕/很   →  slot: 姊姊/蟑螂/怕/很
我的耐心已經消磨殆盡  只丟*: 我/耐心/磨/耗盡/光了  →  slot: 我/耐心/磨/耗盡
這個美女愛上那個醜男人 只丟*: …醜/難看/不漂亮/…    →  slot: …醜/…
```

重現：

```bash
python3 - <<'PY'
import json, sys; sys.path.insert(0,"scripts"); import metrics
recs=[json.loads(l) for l in open("results/v11_test_papers_test.jsonl")]
rows=[json.loads(l) for l in open("data/splits/test_papers.jsonl")]
vocab=set(json.load(open("data/tsl_gloss_vocab.json"))["glosses"])
preds=[r["pred"] for r in recs]
for k,refs in {
 "修正前":[r["ref"] for r in recs],
 "只丟*":["/".join(t for t in r["ref"].split("/") if not t.startswith("*")) for r in recs],
 "slot級":[r["gloss_text"] for r in rows]}.items():
    m=metrics.evaluate(refs,preds,vocab)
    print(k, m["ExactMatch%"], m["ROUGE-L"], m["BLEU-4"])
PY
```

#### 3.4 評估維度：教授要 7 維，已補到 6 維（2026-08-13 完成）

| 教授要求 | 現況 | 位置 |
|---|---|---|
| 語意 | ❌ **自動指標答不了，需母語者 5 分制人工評分** | 計畫 6.2 |
| 選詞 | ✅ Gloss替換錯誤 | `eval_metrics_ext` |
| 語序 | ✅ 語序錯誤＋GER | 同上 |
| 漏詞 | ✅ 漏Gloss | 同上 |
| 亂加詞 | ✅ 多餘Gloss | 同上 |
| 未知詞 | ✅ OOV/未知Gloss | 同上 |
| NMS | ✅ 疑問類型正確率、否定正確率、NMS 產出率 | `eval_json_model.py` |

`eval_metrics_ext` 原本只被 `eval_three_tier.py` 用，一般評估看不到。現已接進
`eval_json_model.py`（線上評估）與 `recompute_from_split.py`（離線重算）。

**OOV 判定基準為「詞彙總表 ∪ 訓練詞彙」（13,663 詞）**，不是訓練詞彙——後者會把「合法但訓練沒出現的手語詞」（畫家、幼稚園、目不轉睛）誤判為模型造詞，實測高估 67%（45 個 vs 實際 27 個）。註：`eval_three_tier.py` 仍以訓練詞彙為基準，其錯誤分類帶此已知偏差，尚未重跑。

#### 3.4.1 v11 分維度結果〔已驗證〕

不需要 GPU：既有 `results/*.jsonl` 已存逐句預測，分類完全離線可算。

```bash
python3 scripts/recompute_from_split.py --results results/v11_test_corpus_test.jsonl \
    --split test_corpus --csv results/v11_test_corpus_per_sentence.csv
python3 scripts/recompute_from_split.py --results results/v11_test_papers_test.jsonl \
    --split test_papers --csv results/v11_test_papers_per_sentence.csv
```

| 錯誤型態 | test_corpus (167) | test_papers (143) |
|---|---:|---:|
| 選詞（Gloss替換） | 47.90% | 37.76% |
| 未知詞（OOV） | 33.53% | 22.38% |
| 漏詞 | 12.57% | 6.99% |
| **語序** | **2.40%** | **4.90%** |
| 亂加詞 | 0.60% | 10.49% |
| 完全錯誤 | 1.80% | 4.20% |
| 正確 | 1.20% | 13.29% |
| | TokenF1 0.536／GER 0.639 | TokenF1 0.616／GER 0.607 |

**這是本次最該拿去驗收的一張表**：語序錯誤在兩個誠實測試集上都只有 **2–5%**，而選詞＋未知詞合計 **60–81%**。

三層診斷（`results/three_tier_report.md`，不同測試集、不同方法）先前已得到同一結論「語序不是瓶頸、病因是詞彙覆蓋」。**兩條獨立證據互相印證**，這比單一數字有說服力。

也直接回答了教授「模型到底學到了什麼」：**學到了語序，沒學到詞彙**。

#### 3.5 母語者實際看過：表已產出，尚未送出

盲測 A/B 評分表產生器 [`scripts/make_human_eval_sheet.py`](scripts/make_human_eval_sheet.py)：模型輸出與參考以 A/B 呈現、順序由句子雜湊決定（可重現），評分者不知孰為機器產出，對照 key 另存不交付。

**修掉一個會擋住執行的 bug**〔已驗證〕：結果檔名原本組成 `v11_test_corpus__test.jsonl`（多一個底線，`load_results(tag, split)` 的兩個參數被合併成一個傳入），三個來源全部找不到檔，直接印「沒有可評估的資料」。修正後實測可產出。

```bash
python3 scripts/make_human_eval_sheet.py --model-tag v11 --n-corpus 60 --n-papers 30 --n-core 20
```

⚠️ 腳本預設 `--model-tag v12`，但 `results/` 內沒有任何 `v12_*` 逐句結果檔（v12 只跑過三層診斷），且對外一律用 v11 → **必須明確帶 `--model-tag v11`**。

只抽「模型輸出 ≠ 參考答案」者，完全相同的沒有判定價值：

| 類型 | 母體 | 與參考相同 | 可抽 | 實抽 |
|---|---:|---:|---:|---:|
| 語料庫長句 | 167 | 2 | 165 | 60 |
| 論文例句 | 143 | 17 | 126 | 30 |
| 核心短句 | 33 | 23 | **10** | 10 |

核心 33 句要 20 題但只剩 10 句可抽——**33 句裡有 23 句模型答得與參考一字不差**，這本身就是「核心短句已被記住、不具鑑別力」的佐證。

輸出 `outputs/人工評估表_v11_盲測.xlsx`（100 題）與 `outputs/人工評估對照key_v11.json`（⚠️ 對照答案，不可交給評分者）。

評分維度為語意正確、語序自然、可否使用三項，**不含 NMS**——那需要看影片，且本模組不輸出 NMS（見第 0 節範圍）。3.4 表中 NMS 那一列是文字層的疑問／否定標記正確率，不等於教授所指的表情動作，驗收時不可混為一談。

**狀態：表已產出，尚未送出、尚未有任何母語者評過。**

---

### 點 4：先比較基線再決定要不要重訓

**現況：這是四點裡唯一「完全沒有數據」的一點。**

〔已驗證〕基線只在核心 33 句上跑過：

```bash
wc -l results/baseline_gemma4_e4b_*.jsonl
python3 -c "
import json
ids={json.loads(l)['id'] for l in open('results/baseline_gemma4_e4b_zero.jsonl')}
for s in ['test','test_corpus','test_papers']:
    t={json.loads(l)['id'] for l in open(f'data/splits/{s}.jsonl')}
    print(s,'重疊',len(ids&t),'/',len(t))"
```

```
33 results/baseline_gemma4_e4b_fewshot.jsonl
33 results/baseline_gemma4_e4b_rules.jsonl
33 results/baseline_gemma4_e4b_zero.jsonl

test        重疊 33 / 33
test_corpus 重疊  0 / 167
test_papers 重疊  0 / 143
```

既有基線數字（`results/summary_gemma4_e4b.json`，n=33）：

| 策略 | EM% | BLEU-4 | ROUGE-L |
|---|---:|---:|---:|
| zero-shot | 27.27 | 39.50 | 55.10 |
| + 規則提示 | 30.30 | 42.56 | 57.86 |
| + few-shot | 36.36 | 44.95 | 65.59 |
| 〔對照〕v11 微調 | 69.70 | — | 82.71 |

**但那 33 句正是 [NEXT_STEPS.md](NEXT_STEPS.md) 自己規定「僅作歷史對照、對外報告不得引用」的資料。** 在兩個誠實測試集（test_corpus 167 句、test_papers 143 句）上，「微調到底有沒有幫助」目前**無法回答**。

#### 執行前必須修的兩個公平性問題

1. **示例池來源**：`EXEMPLAR_IDS = ["P01","P03","P04","P05","S01","S09","S14","S21","S23","S28"]`（`run_baseline.py` 第 56 行）全部來自核心 33 句家族。要在 test_corpus／test_papers 上跑，示例池應改從 train 抽，否則示例與測試分布不同。
2. **推論堆疊不同**：基線走 **Ollama CPU + `think:False`**（`run_baseline.py` 第 83–95 行，註解說明思考鏈會佔滿 `num_predict`），微調模型走 **transformers GPU greedy**（`eval_json_model.py:84`、`serve_model.py:129`，皆 `do_sample=False`，無 beam）〔已驗證〕。同一個底模跑兩套堆疊，基線若輸掉會被質疑「輸在推論設定」。

#### 成本估算〔已驗證〕

由既有結果檔的 `seconds` 欄位實測（Ollama CPU）：

```
zero     5.1 秒/句
rules   14.6 秒/句
fewshot 21.5 秒/句
```

310 句 × 3 策略 ≈ **3.5 小時，可過夜跑**。

---

## 2. 今天實際改了什麼

### 2.1 修正內容

| 檔案 | 改動 |
|---|---|
| [`scripts/extract_paper_examples.py`](scripts/extract_paper_examples.py) | `clean_gloss_line` 改為：先收攏 `/` 兩側與 `*`/`?` 後的空白，再以 slot 為單位丟掉 `*`/`?` 開頭的替代項。docstring 記載病灶、影響句數，以及「只適用論文來源、語料庫的 `?` 不可碰」 |
| [`scripts/reparse_paper_glosses.py`](scripts/reparse_paper_glosses.py) | **新增**。用修正後的解析器從既存 `raw_gloss_line` 重算，含等價性驗證閘門 |
| [`scripts/recompute_from_split.py`](scripts/recompute_from_split.py) | **新增**。換上新參考答案離線重算指標，以索引對齊並驗證中文一致 |
| `data/papers/paper_examples_all.jsonl`、`paper_examples.jsonl` | 43 筆 gloss 更新 |
| `data/splits/test_papers.jsonl` | 39 句參考答案更新 |
| `data/splits_json/test_papers.jsonl` | 重新產生 |
| [NEXT_STEPS.md](NEXT_STEPS.md) | v11 表格更新，附修正紀錄與限制警告 |

### 2.2 為什麼可以不重跑推論

原始 .txt（pdftotext 產出）已不在版控內，但每筆都保留了 `raw_gloss_line`。`reparse_paper_glosses.py` 在寫回前會先驗證「**舊**解析器 + 既存 `raw_gloss_line`」能否重現既存 `gloss_text`：

```
等價性驗證：296/296 筆可由 raw_gloss_line 重現
```

296/296 通過，代表重算與重跑抽取等價。又因論文例句全在測試集，模型輸出完全未變，只需換參考答案離線重算。

### 2.3 重算結果〔已驗證〕

```bash
python3 scripts/recompute_from_split.py \
    --results results/v11_test_papers_test.jsonl --split test_papers
```

```
results/v11_test_papers_test.jsonl  n=143  參考答案有變動 39 句

指標              舊參考     新參考       差
ExactMatch%      11.19     13.29    +2.10
ROUGE-L          55.23     57.08    +1.85
BLEU-4           16.51     17.71    +1.20
InVocab%         12.65     12.65    +0.00
InVocabRef%      15.55     16.86    +1.31
```

**最後一列是獨立佐證**：`InVocabRef%` 是**參考答案自己**的詞彙合法率，它上升了 1.31——因為被誤串進去的 `*見`、`*欣賞` 本來就不在詞彙表裡。這不是模型變好，是答案變對了。

### 2.4 一筆照實記錄的例外

`PAP0205`「愛美」（原始標記 `愛+美 1 / ? 美 2`）正確解析後只剩 `愛+美` 一個 Gloss，被「至少兩個 Gloss 才算句」的既有規則擋下，**保留原值 `愛+美/美`（已知為錯）**。該筆不在任何切分內（論文例句只進 test_papers），不影響任何數字。

### 2.5 對齊方式的已知弱點

`results/v11_test_papers_test.jsonl` 用 `T000`–`T142` 流水號，**不是** `PAP####`，無法直接回溯論文來源。本次以**逐列索引**對齊，並驗證去標點後的中文完全相同（143/143 通過；其中 7 句僅差句尾標點，因評估時會先去標點才餵給模型）。

→ 建議：評估輸出保留原始 id。尚未修改。

---

## 3. 尚未做、以及做不到的事

> 本表於 2026-08-13 深夜更新。凌晨版有多項標「未做」者當日稍後已完成並 commit，下表為現況。

| 項目 | 狀態 |
|---|---|
| 基線在 test_corpus／test_papers 上的結果 | ✅ **已做**（commit 63f7edd）。930 次生成，基線改走 transformers GPU 與微調同一套推論堆疊（618ddb9） |
| 評估維度 3 維 → 7 維 | ✅ **已補到 6 維**（commit 6b437f0）。缺的「語意」自動指標答不了，須靠 3.5 的人工評估 |
| MODEL_CARD 更新到 v11 | ✅ **已做**（commit e59f52c，v11） |
| 訓練資料抽查表（教授第 2 點後半） | 🔶 **表已產出**（見 2.5），尚未送出、尚未有人評過 |
| 母語者人工評估（計畫 6.2） | 🔶 **表已產出**（見 3.5），尚未送出、尚未有人評過 |
| `agreement` 欄位灌入論文方向標記 | **未做**，且本週不做（見 2.4，是主動取捨） |
| 重訓 v13 | **不做**。v11／v12 凍結——教授明講「先不要一直重訓新版本」 |
| 中文端改寫增強、教育部辭典擴詞、上下文翻譯續作 | **不做**，列入未來工作 |
| Cloudflare Tunnel | **不做**，改 SSH tunnel 現場 demo |
| dev 的 8 句中文重疊、45 句標籤噪音 | **未處置**（見 3.1、3.2） |
| `eval_three_tier.py` 的 OOV 基準偏差 | **未重跑**（見 3.4 註） |
| 評估輸出保留原始 id | **未修**（見 2.5 對齊弱點） |

### 3.1 兩張表都還沒有人看過——這是目前最大的缺口

教授四點回饋裡，**唯一無法靠寫程式解決的就是「請台灣手語使用者或老師實際看過」**，而它同時出現在第 2 點（抽查訓練資料）和第 3 點（評估模型輸出）。

現在兩張表都在 `outputs/`，加起來 250 題。在有人評過之前：

- 「模型學到了什麼」只能答到自動指標能答的部分（語序 ✅、詞彙 ❌），**語意正確性一律不可宣稱**
- 訓練資料的 Gloss 正確率是**未知數**，不是「已審核」

驗收時這兩句要主動講，不要等人問。

---

## 4. 待決事項

1. **基線在哪裡跑**：本機無 Ollama。選項為（a）改好腳本交由 VM 過夜跑、（b）提供 SSH 由我執行、（c）本機起 Ollama（Mac CPU 會遠超 3.5 小時，不建議）。
2. **基線 zero-shot 要不要改走 transformers GPU**：好處是與微調模型同一套推論堆疊，能回答「是不是輸在推論設定」；代價是多一支腳本並要與共用 GPU 的 root 服務排時間。若不做，報告須明白註記基線為 CPU + `think:False`。
3. **45 句一對多標籤**是否合併。
4. **dev 的 8 句中文與 train 重疊**是否清除。

---

## 附錄 A：如何從零查驗

`data/splits/*.jsonl` 與 `data/splits_json/*.jsonl` **不入版控**（見 `.gitignore`，可由 `split_data.py` 再生）。因此查驗分兩層：

### A-1　只用版控內的檔案就能驗（不需重生切分）

`data/papers/paper_examples_all.jsonl` 與 `results/*.jsonl` 都在版控內，所以本文件的**核心宣稱**可以直接驗：

```bash
# 1. 解析修正的等價性與影響句數（296/296 重現、43 筆變動）
git stash                                   # 退回修正前
python3 scripts/reparse_paper_glosses.py    # 應報 43/296
git stash pop

# 2. 43 筆到底改了什麼（逐行 diff，papers 檔在版控內）
git diff data/papers/paper_examples_all.jsonl
```

### A-2　需要先重生切分（才能驗指標）

```bash
python3 scripts/split_data.py --use-all --length-balance --papers-as-test \
    --corpus-test-ratio 0.12 --corpus-test-min-len 6
python3 scripts/build_json_targets.py --splits train dev test test_corpus test_papers
python3 scripts/recompute_from_split.py \
    --results results/v11_test_papers_test.jsonl --split test_papers
```

`recompute_from_split.py` 內建對齊閘門：逐列比對去標點後的中文，任一列不符即中止並要求重跑推論，**不會**在切分已變動的情況下悄悄算出一個錯的數字。

### A-3　查驗不到的部分

| 項目 | 原因 |
|---|---|
| adapter 權重、訓練 checkpoint | 在訓練 VM 上，未入版控（見 `.gitignore`）。可依 MODEL_CARD 的配方重跑得到 |
| 論文原始 .txt（pdftotext 產出） | 已不在版控內。故本次採「從 `raw_gloss_line` 重算 + 等價性閘門」而非重跑抽取 |
| 教育部辭典爬取資料 | 授權為可訓練、**資料不得公開**，故不入版控 |

---

## 附錄 B：前次會期結論的複驗結果

本次逐項抽驗前一份分析的宣稱，結果如下：

| 宣稱 | 複驗 |
|---|---|
| test_papers 33 句參考答案含 `*` | ✅ 精確 |
| 修正後 EM 11.19% → 13.29% | ✅ 獨立重算，分毫不差 |
| `*`/`?` 分佈 train 41／dev 5／test_corpus 2／test_papers 33 | ✅ 精確 |
| 前三者全為 tslcorpus、`?` 為疑問句尾標記 | ✅ 精確，另含 2 筆轉寫缺字 |
| paper_examples_all.jsonl 296 句、`*` 35 句、`?` 0 句 | ✅ 精確（以 `gloss_text` 計） |
| 基線是 Ollama CPU 跑的 | ✅ `run_baseline.py:83` `call_ollama`、`think:False` |
| 推論為 greedy 無 beam | ✅ `eval_json_model.py:84`、`serve_model.py:129` 皆 `do_sample=False` |
| checkpoint 依 dev loss 挑、`save_total_limit=None` 全留 | ✅ `train_qlora.py:228` 註解寫明 |
| 合成句 rule-derived 609／attested-pattern 337／corpus-attested 21 | ✅ 精確（`data/synth/tsl_synth.jsonl`）。註：進入切分後 attested-pattern 為 334，差 3 筆 |
| 852 句 `pending` 但帶老師 7/24 審核結論（523/176/87/66） | ✅ 精確，且 967 句皆有 `teacher_train_eligible` 欄位 |

**一處需修正**：「論文的 `?` 有零句、所以只需處理 `*`」——對**字元**成立，對**問題**不成立。`?` 標記的替代詞因標記與標的之間有空白而存活下來（11 句）。此即本次採 slot 級解析而非 token 級過濾的原因。

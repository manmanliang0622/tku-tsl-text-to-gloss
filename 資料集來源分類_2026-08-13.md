---
title: 資料集來源分類（資料是從哪裡抓下來的）
updated: 2026-08-13
---

# 資料集來源分類

把專案裡每一批資料對上「從哪裡抓的、用什麼腳本抓的、落在哪個檔、有沒有進訓練」。
數字皆為 2026-08-13 由本機檔案實際點算。

授權條件見 [資料來源.md](資料來源.md) 檔首「授權更新（2026-08-04）」。

---

## 總覽

| # | 來源 | 取得方式 | 落點 | 原始筆數 | 進訓練？ |
|---|---|---|---:|---:|---|
| A | 文化部臺灣手語語料庫（測試版） | 公開 API 全爬 | `data/tslcorpus/parallel.jsonl` | 5,272 句 | ✅ 最大宗 |
| B | 中正大學台灣手語線上辭典（第五版）· 例句 | 公開 API 全爬 | `data/twtsl/twtsl_sentences.jsonl` | 544 句 | ✅ |
| C | 規則模板合成句 | 本地生成，非爬取 | `data/synth/tsl_synth.jsonl` | 967 句 | ✅ |
| D | 自有標記句 | 人工建立，非爬取 | `data/tsl_sentences.jsonl` | 35 句 | ❌ 只當 `test` |
| E | 中正大學手語研究論文例句 | PDF 手動下載＋腳本抽取 | `data/papers/paper_examples*.jsonl` | 296 → 去重 153 | ❌ 只當 `test_papers` |
| F | 中正辭典 · 詞條 | 公開 API 全爬 | `data/twtsl/twtsl_words.jsonl` | 3,500 詞條 | ❌ 只建詞彙表 |
| G | 教育部常用手語辭典 | 未公開文件的 API，逐字列舉 | `data/moe/moe_words.jsonl` | 15,325 → 清理 8,438 | ❌ 只建詞彙表 |

**「進訓練」的只有 A/B/C 三個來源**；D/E 是留存測試集，F/G 是拿來判斷「模型產出的詞是不是合法手語詞」的字典，不是語料。

---

## 一、爬下來的真實平行語料

### A. 文化部臺灣手語語料庫（測試版）— 專案的主力資料

- **來源網址**：<https://tslcorpus.moc.gov.tw/>
- **抓法**：[`scripts/scrape_tslcorpus_full.py`](scripts/scrape_tslcorpus_full.py)，走網站前端同款的公開 API（`https://tslcorpus.moc.gov.tw/api`）全爬。
- **原始落點**：`data/refs/tslcorpus_raw.jsonl`（未入版控）→ 整理成 `data/tslcorpus/parallel.jsonl`，**5,272 句**中文↔Gloss 平行句。
- **性質**：聾人自然對話的篇章語料，句子長、有上下文相依（同一段對話用 `seg_uuid` 群組化）。
- **授權**：訓練＋散布皆合法，條件是標明出處「臺灣手語語料庫（測試版），文化部」。

進切分後（相異句對）：

| 切分 | 相異句對 | 過取樣後列數 |
|---|---:|---:|
| train | 4,016 | 6,817 |
| dev | 493 | 493 |
| **test_corpus**（留存） | **167** | 167 |

### B. 中正大學台灣手語線上辭典（第五版）· 例句

- **來源網址**：<https://twtsl.ccu.edu.tw/>
- **抓法**：[`scripts/scrape_twtsl.py`](scripts/scrape_twtsl.py)，公開 API（`https://twtsl.ccu.edu.tw/api`）。
- **落點**：`data/twtsl/twtsl_sentences.jsonl`，**544 筆去重例句**（API 原始 546 筆）。
- **授權**：訓練＋散布皆合法，須標明出處（蔡素娟、戴浩一、劉世凱、陳怡君。2026。《台灣手語線上辭典（中文版第五版）》。嘉義：國立中正大學手語語言學台灣研究中心）。
- 進切分：train 496 相異句對（929 列）／dev 44。

> ⚠️ 官網寫「4,600 詞、560 個常用詞之例句」，API 回的是 3,500 詞條 ID／546 例句——**是統計口徑不同，不是漏抓**。

---

## 二、沒有爬、是自己做的

### C. 規則模板合成句

- **產生方式**：[`scripts/synthesize.py`](scripts/synthesize.py)，用 7 條語法規則＋槽位模板生成。**沒有外部來源可爬**，每句都在 `rule_basis` 欄記錄推導依據（引 Tai & Tsay 2015、張榮興 2008、文化部語料庫實例等）。
- **落點**：`data/synth/tsl_synth.jsonl`，**967 句**。
- 證據強度分層：`rule-derived` 609 ／ `attested-pattern` 337 ／ `corpus-attested` 21。
- 經手語老師 2026-07-24 於 gloss 層審核（108 句修正、7 句待影片者排除）。
- 進切分：train 835 相異句對（1,246 列）／dev 129。

### D. 自有標記句

- **產生方式**：專案自製並人工標註，35 句／38 詞（`data/tsl_sentences.jsonl`、`data/tsl_words.jsonl`）。
- 其中 33 句（排除 S24、S26 兩句未展開模板）固定當 `test` 核心測試集，**永不進訓練**。

---

## 三、從論文 PDF 抽出來的（留存測試集）

### E. 中正大學臺灣手語研究論文例句

- **來源**：<https://taiwansign.ccu.edu.tw/> 上公開的論文 PDF，**人工下載，非 API 爬取**。
- **抽取腳本**：[`scripts/extract_paper_examples.py`](scripts/extract_paper_examples.py)，把論文格式（空格分隔 Gloss、`/` 表替代詞、下標數字表呼應變體）轉成專案格式。
- **落點**：`data/papers/paper_examples_all.jsonl`（296 句）→ `paper_examples.jsonl`（去重 153 句）→ `data/splits/test_papers.jsonl`（143 句）。

`test_papers` 143 句的論文出處分布：

| 論文 | 句數 |
|---|---:|
| 魏彥宜 2022《臺灣手語同義詞研究》 | 117 |
| 論文 `thesis_wencheng` | 12 |
| 戴浩一、蘇秀芬 2006《台灣手語的呼應方式》 | 10 |
| 陳泱儒《台灣手語篇章中韻律標記與句法結構的對映》 | 4 |

- **完全不進 train/dev**（各 0 筆），是與語料庫來源完全不同的第二個留存測試集。
- 2026-08-13 修正：39 句參考答案曾誤把論文的替代詞串成 Gloss 序列，重新解析後離線重算指標（EM 11.19%→13.29%）。

---

## 四、只拿來當「合法詞彙判定」的字典（不是語料）

這兩批是詞典型資料：一個中文詞對一支示範影片，中文詞本身就是 Gloss 標籤，**餵進訓練只會強化「照抄中文」**——那正是三層診斷找到的病因。所以只用來判斷模型輸出的詞是不是合法手語詞。

### F. 中正辭典 · 詞條

- 同 B 的爬蟲與 API，`data/twtsl/twtsl_words.jsonl`，**3,500 個詞條 ID／4,636 個名稱索引**。
- 原始回應留在 `data/twtsl/raw_details.jsonl`（3.3 MB，斷點續跑用，未入版控）。
- 併入 `data/vocab/gloss_master.jsonl`（7,002 個詞形）：來源標記 `corpus` 4,680／`twtsl` 2,828／`twtsl-alias` 898／`own` 85。

### G. 教育部常用手語辭典

- **來源網址**：<https://special.moe.gov.tw/signlanguage>
- **抓法**：[`scripts/scrape_moe_signdict.py`](scripts/scrape_moe_signdict.py)。該站 API 未有官方文件，是**從瀏覽器網路請求觀察得到**的（`api/Vocabularies?key=`），且為子字串比對，因此用逐字列舉的方式窮舉。
- **落點**：`data/moe/moe_words.jsonl`（15,325 筆）→ [`scripts/merge_moe_vocab.py`](scripts/merge_moe_vocab.py) 清理成 `data/moe/moe_vocab_clean.jsonl`（8,438 詞，帶來 6,549 個新詞）。
- **授權不同於 A/B**：可用於訓練，但**資料本身不得公開**，故 `data/moe/` 整個目錄在 `.gitignore` 內，不入版控。

---

## 五、下游衍生檔（不是新來源，是上面幾批的加工）

| 檔案 | 由誰產生 | 內容 |
|---|---|---|
| `data/splits/*.jsonl` | `split_data.py` | train 8,992 列／dev 666／test 33／test_corpus 167／test_papers 143。按對話 `seg_uuid`、辭典詞條、模板整組留存防洩漏 |
| `data/splits_json/*.jsonl` | `build_json_targets.py` | 上表轉成 JSON 目標格式（含疑問／否定／NMS 欄位） |
| `data/vocab/gloss_master.jsonl` | `build_vocab.py` | 7,002 詞形，由**完整**語料庫建成（含 test_corpus，故評估時不可直接用） |
| `data/vocab/eval_vocab.json` | `build_eval_vocab.py` | 評估專用詞彙表，只用 train 可見範圍＋辭典，避免洩漏 |
| `data/eval_tiers/*.jsonl` | `build_eval_tiers.py` | 三層診斷用的 seen／paraphrase／unseen 各 100 句 |
| `data/refs/tslcorpus_evidence.jsonl` | 語料庫爬蟲 | 1,082 筆規則佐證句，供 `verify_rules_corpus.py` 查證 |

---

## 六、版控狀態（哪些抓下來的東西不在 repo 裡）

`.gitignore` 排除、需要重跑腳本才會有的：

- `data/twtsl/raw_details.jsonl`、`data/refs/tslcorpus_raw.jsonl` — 原始 API 回應，可再抓
- `data/moe/` 整個目錄 — **授權限制，資料不得公開**
- `data/splits/*.jsonl`、`data/splits_json/*.jsonl` — 可由 `split_data.py` 再生，只保留 `manifest.json` 供追溯
- `data/vocab/eval_vocab.json` — 可由 `build_eval_vocab.py` 再生

重建順序：

```bash
python3 scripts/scrape_tslcorpus_full.py && python3 scripts/scrape_twtsl.py && python3 scripts/scrape_moe_signdict.py && python3 scripts/merge_moe_vocab.py && python3 scripts/extract_paper_examples.py && python3 scripts/split_data.py --use-all --length-balance --papers-as-test --corpus-test-ratio 0.12 --corpus-test-min-len 6 && python3 scripts/build_json_targets.py --splits train dev test test_corpus test_papers
```
